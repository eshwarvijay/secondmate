## 2026-09-24 — audit-trail-restructure: restructure secondmate's audit trail into a lookup-only, generic mechanism (bin/audit-log.py)

- **Trigger:** the audit trail itself (`audit/flow.md`/`audit/decision.md`, both `@`-imported in full via `CLAUDE.md`) had grown to 171,886 / 126,520 characters and was unboundedly auto-loading into every session's context. Human binding decisions: (1) full lookup-only restructure for BOTH files — nothing full-text auto-loads except a small bounded index; (2) a generic, plugin-shipped mechanism (matching `bin/lesson-lookup.py`'s precedent), not a one-off fix to this repo's own files, since `skills/secondmate/SKILL.md` step 10 ships to every repo installing secondmate.
- **Process note (why this entry is written by a sub-supervisor, not the usual solo loop):** the top-level supervisor created the worktree and started the Claude maker directly, itself, without going through the fan-out pattern's claim-ledger mechanism — a real process mistake, caught and corrected mid-task by handing supervision (Check onward) to a freshly-spawned sub-supervisor. The sub-supervisor claimed the task-id, ran the remainder of the loop (Check → Gate → Hold → Integrate → Teardown → this entry), released its claim on a genuine stuck-on-hold path when the human hadn't answered within the standard ~5-minute poll window, then was resumed via a coordinator message once the hold was answered — re-verifying the answer against the raw `decisions.jsonl` ledger before acting, not trusting the resume message alone.
- **Maker path:** Claude (`--permission-mode auto`), already running in dedicated worktree workspace `w3W`, root pane `w3W:p1`, branch `sm/audit-trail-restructure` — started directly by the top-level supervisor before the fan-out hand-off; the sub-supervisor adopted the in-flight maker rather than restarting it.
- **Worktree:** `herdr worktree create` → `sm/audit-trail-restructure` (workspace `w3W`).
- **Rounds:** 6 checker rounds (`gpt`, `qa/coverage` + `qa/behavioral-contracts` + `redteam/injection` lenses, run in the strict visible checker pane per the skill's own rule — split off the worktree's root pane, with an explicit `cd` into the worktree inside the checker script, since this repo's own audit trail already documents that a bare `herdr pane run` does not reliably inherit the split pane's cwd). Rounds 1-5 each found genuine, independently-reproduced-by-the-supervisor bugs in the maker's first delivery (`bin/audit-log.py`) before any of them were routed back — every fix was also independently re-verified and mutation-tested (revert the fix, confirm the corresponding new selfcheck assertion then genuinely fails with the predicted symptom, restore, reconfirm clean) before trusting it:
  1. `show --task <task-id>` couldn't find any of the 62 real migrated legacy entries (filename glob required an exact `--<slugified-task-id>.md` suffix, but migrated files are named after the slugified TITLE, since legacy entries never had a discrete task-id) — fixed to substring-match against the filename slug, printing every match; `migrate` silently truncated an entry at any in-body markdown horizontal rule (`---` was treated as an entry boundary everywhere, not just between entries) — fixed by splitting on entry HEADER lines instead; `migrate` silently dropped a real entry when its derived filename collided with a DIFFERENT pre-existing file's content, reporting false success — fixed with a content comparison before treating a collision as idempotent; `migrate` didn't preserve CRLF line endings byte-verbatim (`read_text()`/`write_text()` do universal-newline translation) — fixed with `newline=""`; `audit/INDEX.md` wasn't actually size-bounded against a single entry with a pathologically long `--title` (the entry-count cap alone didn't bound one entry's title length) — fixed with a `_truncate()` helper capping each rendered index line.
  2. A narrower sibling of bug 1's horizontal-rule fix: a trailing in-body `---` was still stripped when it belonged to the LAST entry (no next header follows it) — fixed by only stripping a trailing separator when there IS a next entry; `migrate` also unconditionally appended a trailing newline even when the source's last entry genuinely had none, growing the file by one byte — fixed by removing the forced append; `_truncate` itself returned up to 3 characters MORE than its own configured cap (sliced to `max_len` THEN appended "..." on top) — fixed to reserve room for the ellipsis within the cap.
  3. The committed `audit/INDEX.md` was a stale generated artifact, still reflecting the pre-fix `_truncate` behavior because a fresh `reindex` was never re-run after the round-2 fix landed — regenerated and committed; `cmd_search` lacked the same `--type flow|decision` validation every sibling subcommand (`add`/`show`/`list`/`migrate`) already had, silently returning "no matches" for a typo'd type instead of rejecting it — fixed, while keeping `--type` optional for `search`.
  4. Two DIFFERENT, both-valid task-ids ("a_b" and "a-b") collided on the same derived filename because `cmd_add`/`cmd_show` ran the already-validated task-id through the lossy `_slugify()` helper (designed for arbitrary title text, not an already-safe identifier) — fixed with a new `_task_component()` helper using the validated task-id directly; a pre-existing selfcheck assertion (`assert code == 0 and "no matches" not in out.lower() or True`) had been silently vacuous since round 0 due to Python operator precedence (`(A and B) or True` is always `True`) — fixed to pin the real intended behavior.
  5. A sibling of bug 4 one dimension deeper: `_task_component()`'s `.lower()` call let two task-ids differing only by case ("Task-A"/"task-a") still collide — the supervisor independently confirmed the underlying filesystem is itself case-INSENSITIVE (a plain macOS default), so normalizing case in Python code alone could not have actually fixed this; closed at the source by tightening `_TASK_ID_RE` to lowercase-only, matching this repo's own real-world convention (every one of its 62 historical task-ids was already lowercase-kebab-case).
  6. **Clean pass, zero findings** — the checker independently re-verified, from scratch, that all 62 real historical entries are byte-identical to the original monoliths, that the committed `INDEX.md` matches a fresh `reindex`, and swept for a fourth dimension of the same collision class (Unicode/whitespace/other characters) without finding one.
- **Outcome:** verify-gate PASS at `d6c01f9512a509f0c718fa78e5da276767165cd6` (checked-sha match) → human hold `a3d6bec9` (bound to that sha) → genuinely unanswered for ~5 minutes of polling, sub-supervisor stopped and reported `SM_STUCK_NEED_HUMAN` rather than guess → answered `merge` (confirmed by the sub-supervisor against the raw `decisions.jsonl` record, not just the resume message) → merged to main via `bin/merge-sequencer.sh` at `98c4f0389a7b45e3b9ed20641dc9ff1446893449`, pushed → full repo-wide selfcheck chain (including this same task's own `bin/audit-log.py selfcheck`) re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed → worktree/workspace/branch torn down.

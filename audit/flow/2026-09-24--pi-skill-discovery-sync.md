## 2026-09-24 — pi-skill-discovery-sync: close pi-vs-Claude-Code skill discovery gap + harden doctor.sh heal-fix against symlink escapes/TOCTOU (fan-out mid-task handoff)

- **Trigger:** a root-caused, verified gap in secondmate's own shipped tooling: pi (`@earendil-works/pi-coding-agent`) only discovers skills from `.pi/skills/` (project), `.agents/skills/` (the neutral "Agent Skills" standard, walked from cwd to repo root), or an explicit `skills:[...]` array in `.pi/settings.json` — it never scans `.claude/skills/`. Claude Code scans `.claude/skills/` and `.agents/skills/`, never `.pi/skills/`. `bin/sync-worktree-skills.sh` (already shipped) only backfilled `.claude/skills/` into fresh worktrees — a complete no-op for pi makers.
- **Handoff / process note:** this task was originally started directly by the top-level supervisor (a maker agent launched without going through the fan-out pattern's claim-ledger/sub-supervisor mechanism) — a real process mistake, caught and corrected by handing supervision (Check onward) over to a freshly-spawned sub-supervisor mid-flight, adopting the maker's real in-flight progress rather than restarting it.
- **Maker path:** Claude (Sonnet 5, `--permission-mode auto`), dedicated worktree workspace `w3X`, root pane `w3X:p1` — already running when the sub-supervisor took over.
- **Worktree:** `herdr worktree create` → `sm/pi-skill-discovery-sync`.
- **Rounds:** 6 checker rounds (`gpt`, `qa/coverage` + `qa/test-reality` + `redteam/injection` lenses, run in the strict visible pane per this repo's standing rule), every round finding and fixing a real, independently-reproduced defect except the final clean pass:
  1. `_detect_skill_discovery_sync` (the new `bin/doctor.sh` diagnostic) was top-level-only, silently missing nested monorepo skills that `bin/sync-worktree-skills.sh` already syncs at any depth — reproduced directly (a `packages/widget/.claude/skills/nested-only` fixture produced zero rows) before and after the fix (any-depth `os.walk`, rows keyed by `(relative-parent-prefix, name)` so two subprojects can share a skill name without collision).
  2. **Most severe finding:** the heal-fix's destination path was never containment-checked (only the source was) — a symlinked `<prefix>/.agents` pointing outside the repo let `--heal --yes` write real skill content outside the checkout. Reproduced directly (a real external tmp dir received the leaked file) before and after the fix (walk to the nearest existing ancestor of the destination, containment-check it the same way the source already is).
  3. A real TOCTOU in round 2's own fix: containment was only checked at fix-string-*generation* time, but the returned string is documented as inspectable/deferrable (readable via `--json`, runnable by a human later), so a filesystem change between generation and execution could still leak content via an already-generated, now-stale fix string. Reproduced directly (generate safe, mutate the destination ancestor into an external symlink, run the stale string — leaked) before and after the fix (containment re-validated inside the executed command itself, via an embedded `python3 -c`, recomputed fresh at execution time rather than trusting the earlier decision).
  4. One confirmed test-coverage gap (the execution-time re-check already correctly refused a stale *source* too, but nothing exercised that path — closed with a symmetric test) and one SUSPECTED, narrower residual risk (the embedded python's own check-then-mkdir/cp is still two syscalls in one process, so a theoretical adversary with concurrent local write access could race the sub-instant window between them) — the sub-supervisor made the disclosed judgment call to document this as an accepted limitation rather than chase it further, citing this repo's own precedent (`bin/scope-guard.py`'s documented symlink TOCTOU, `bin/caffeinate-guard.sh`'s PATH/supply-chain trust assumption, this same session's `git-coordinator` round-14 precedent) — the following checker round accepted this framing without re-litigating it.
  5. A second small, confirmed coverage gap: the embedded python's generic `except` around `mkdir`/`cp` (e.g. if `cp` is missing) was reachable and already correctly fail-closed, but untested — closed with a pure test addition, no production change. Independently reproduced (a PATH restricted to every tool the fix needs except `cp`) before and after.
  6. Clean pass, zero findings, explicit confirmation that `bin/sync-worktree-skills.sh`/`bin/new-worktree.sh` remained byte-identical to the round-1 commit and that docs/version stayed in sync.
- **Supervisor verification discipline:** every one of the 5 fixes above was independently reproduced with a from-scratch fixture (real symlink escapes, a real generate-then-mutate-then-execute TOCTOU sequence, a real PATH-restricted `cp`-missing probe) both BEFORE routing to the maker and AFTER the fix landed, and every new selfcheck assertion was mutation-tested (temporarily neutering the fix, confirming the assertion then genuinely failed with the predicted symptom, restoring via `git checkout --`, reconfirming a clean full selfcheck) before being trusted. One real process mistake, caught and corrected mid-task: running two `bin/doctor.sh --selfcheck` invocations concurrently (a clean post-fix run racing an in-place mutation-test run) produced a spurious, unrelated failure (`Test P5 kimi-k3 UNKNOWN`) — diagnosed as a self-inflicted concurrency artifact (not a real regression) once redone strictly sequentially with no overlapping file access, matching this repo's own established discipline of re-verifying every result rather than trusting a single ambiguous run.
- **Genuine hold-wait-resume cycle, not self-answered:** opened hold `1442ae0c` bound to the final checked sha, polled `bin/hold.py open` every ~30s for several minutes with no answer, released the claim, and reported `SM_STUCK_NEED_HUMAN:1442ae0c` as the sub-supervisor's own turn-ending signal rather than guessing. On resumption (relayed by the coordinator), independently re-verified the answer against the raw `decisions.jsonl` ledger (`{"ev":"hold",...,"sha":"d94894a..."}` followed by `{"ev":"answer","id":"1442ae0c","a":"merge"}`) AND `bin/hold.py open` (empty) before re-claiming the task-id and proceeding — never trusted the resuming message's claim at face value.
- **Discovered, deliberately deferred (not fixed in this task):** `bin/sync-worktree-skills.sh`'s own `_sync_one_skill` has the identical source-checked/destination-unchecked asymmetry — a worktree whose top-level `.claude`/`.pi`/`.agents` is itself a symlink pointing outside the worktree would have its `mkdir -p`/`mv` follow it and leak content externally. Confirmed real via direct reproduction, and confirmed byte-identical between `main` and this task's every commit (pre-existing, untouched by this diff) — explicitly kept out of this task's scope per the sub-supervisor's own instruction to the maker, and flagged to the coordinator as a separate follow-up.
- **Outcome:** verify-gate PASS at `d94894a69d257d6bc467cbd47fdc3863d751a457` (checked-sha match) → human hold `1442ae0c` answered `merge` (genuine, independently re-verified from the raw ledger before acting) → merged to main via `bin/merge-sequencer.sh` at `e809fcd8060bae685d3f2a09d5e1fe7f001e716e`, pushed → full selfcheck chain (`sync-worktree-skills.sh`, `new-worktree.sh`, `doctor.sh`) re-run clean, sequentially, on the actual merged commit in the primary checkout → worktree/workspace `w3X`/branch torn down → claim released.
- **Lesson:** this task's most valuable discipline was refusing to trust a single ambiguous selfcheck run — the concurrent-mutation-test mistake produced a real, unrelated-looking failure that could easily have been misdiagnosed as a genuine regression in an unrelated Bedrock-override feature; redoing the exact same check strictly sequentially (no overlapping file access) immediately resolved the ambiguity. Also: this task is a second real exercise (after `fix-doctor-status-line`/`fix-scope-guard-marker-doc`) of the fan-out pattern's genuine hold-wait-resume cycle, including a mid-task supervisor handoff from the top-level supervisor directly to a sub-supervisor — adopting an already-in-flight maker's real progress rather than restarting it worked cleanly, and independently re-verifying a resume message against the raw ledger (not just trusting it) caught nothing wrong this time, but is exactly the check that would have caught it if something had been.

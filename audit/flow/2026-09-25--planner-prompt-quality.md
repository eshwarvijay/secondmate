## 2026-09-25 — planner-prompt-quality: evidence-cited upgrade to the 6 plan-committee system prompts

- **Maker path:** Claude, dedicated worktree workspace `w4B`, root pane `w4B:p1`.
- **Worktree:** `herdr worktree create` → `sm/planner-prompt-quality`.
- **Trigger:** live-session follow-up — the user asked to check the starred GitHub repo with a full system-prompt collection (`x1xhlol/system-prompts-and-models-of-ai-tools`) plus other prompt-technique repos (`kingbootoshi/directional-prompting`, `sarthakrastogi/quality-prompts`) to improve `bin/plan-committee.sh`'s 6 planner system prompts, after the supervisor's first read of them showed they were already fairly detailed (not "vague" as initially assumed) — a clarifying `AskUserQuestion` confirmed the user still wanted the existing prompts upgraded, not a different target.
- **Research (delegated to a fork):** found concrete, evidence-cited patterns targeting this repo's own documented planner failure modes rather than generic prompt-quality advice — (1) directional-prompting's negation-cleanup rubric was inconsistently applied (some sentences kept a redundant "Do NOT X" clause beside its own positive replacement); (2) Contrastive CoT (from `quality-prompts`) as the real fix for security-theater/hallucination, using this repo's own logged past mistakes as the INVALID exemplar; (3) a domain-applicability gate (inverted from Devin AI's real "check the library is used first" line, since these planners run `--no-tools`) requiring "N/A -- no such surface" instead of an invented finding; (4) an `ASSUMED:` prefix convention (adapted from Cursor's real "state assumptions and continue" line) for any claim the planner can't verify against the literal task text; (5) an honest negative finding — no real system prompt had a structural fix for glm5's occasional 1-line non-answers, correctly left to this repo's existing synthesis-time discard practice rather than chased via prompt text.
- **Planner committee:** skipped — the research findings were already a complete, concrete spec (specific sentences to change, specific examples to add), matching this session's precedent for skipping the committee when investigation has already resolved the design questions.
- **Rounds:** 3 checker rounds (`global.openai.gpt-5.6-terra`, `qa/coverage` lens), 2 real confirmed bugs plus 1 real coverage gap, all independently reproduced/verified by the supervisor before or after routing:
  1. **(round 1, 2 real bugs):** the qwen3-80b Contrastive-CoT INVALID example was fabricated — it depicted a scenario (recommending the `jsonschema` pip package + a `SchemaValidator` abstraction + citing a nonexistent `bin/schema-utils.py`) that never actually happened, directly contradicting the original instruction to use this repo's own real, already-logged past mistake. The supervisor independently confirmed the real incident via `grep` against `audit/flow/2026-09-11--fix-scope-guard-marker-doc...md`: qwen3-80b once recommended a shared `bin/utils/` function citing nonexistent prior art (`verify-worktree.ts`, `clean.ts`). Separately, the mistral-large3 Contrastive-CoT VALID example self-contradicted the very domain-applicability gate it was meant to demonstrate — its "VALID" finding claimed "file paths derived from a CLI arg" for a task text that only said the flag "prints planned file writes," inventing an unstated mechanism. Both fixed round 2: qwen3-80b's example replaced with the real documented incident; mistral's VALID example re-grounded to an Information Disclosure finding derivable purely from the stated "prints planned file writes" text, with no invented CLI-arg input.
  2. **(round 2, 1 real bug, 1 finding adjudicated not-a-bug):** the new selfcheck assertions for both fixed examples only checked generic structural markers (INVALID/VALID headers, N/A gate phrase) — a future edit could silently regress either example back to its round-1 fabricated form and `--selfcheck` would still report `ok`. Fixed with content-anchored assertions (presence of the real incident's specific details, absence of the fabricated details) for both examples, each mutation-tested by the maker and independently re-verified by the supervisor with a from-scratch mutation (confirmed the mistral guard genuinely fails when the fabricated phrase is reintroduced, and passes clean when restored). A second round-2 finding — that qwen3-coder and kimi-k3 received the new `ASSUMED:` convention despite one clause in the original task instruction saying "do not touch beyond the negation cleanup" — was adjudicated by the supervisor as **not a bug**: the same instruction's own parenthetical explicitly said "point 4's ASSUMED: convention applies to all 6," so the maker correctly implemented the supervisor's actual stated intent; the checker had read only the ambiguous half of a self-contradictory spec sentence written by the supervisor, not a real defect in the diff.
  3. **Round 3: clean pass, zero findings**, after the checker independently re-ran its own mutation test of the mistral regression guard and confirmed it holds.
- **Supervisor verification discipline:** every finding was independently reproduced before being trusted — grepped the real historical qwen3-80b incident from the audit trail before routing the fix; re-read both self-contradicting examples directly against their own stated task text before agreeing they were bugs; ran `bin/plan-committee.sh --selfcheck` directly (not trusted from the maker's claim) after every round; personally mutation-tested the new regression-guard assertion from scratch (first attempt used a fragile string-replace that silently no-op'd due to a substring mismatch — caught by verifying the mutation actually changed the file via `grep` before trusting the selfcheck result, then redone with a reliable line-indexed mutation that correctly triggered the expected failure).
- **Outcome:** verify-gate PASS at `e58ccd64bb9ec1edc7ad69463b4fbeb91294fbe0` (checked-sha match) → human hold `13caa011` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `ad8eec3`, pushed → worktree/branch/pane torn down.
- **Lesson:** a mutation test is only as trustworthy as the verification that the mutation actually applied — the supervisor's own first mutation attempt this task silently no-op'd (a Python string-replace against a substring that didn't exactly match due to a different sentence boundary than assumed) and both the "mutated" and "restored" selfcheck runs printed `ok`, which could easily have been misread as "the guard doesn't work" or worse, trusted as "the guard is fine" without noticing the file was never actually changed. Confirming the mutation landed (a direct `grep`/`diff` check) before trusting either side of a before/after mutation-test comparison is now worth treating as a standing step, not an optional nicety.

## 2026-09-24 — maker-restart-handoff: round-state.md structured handoff file (P1) + loop-guard.sh RESTART/exit-5 signal (P2)

- **Trigger:** direct follow-up to a starred-repo research pass on long-horizon agentic task patterns (ralph, opencode, goose, amplio, deepagents, cwc-long-running-agents), grounded in this repo's own real context-degradation incident (`doctor-plugin-update`: "restarted fresh... at the start of nearly every round" was an ad-hoc fix, never systematized). The research proposed 5 prioritized proposals (P1-P5); the human approved building P1+P2 first ("two halves of one mechanism").
- **Maker path:** pi (Qwen3-Coder-Next, high thinking), dedicated worktree workspace `w3T`, root pane `w3T:p1`. Task run entirely by a fork given full autonomous ownership through merge/push/teardown (matching the `lesson-store`/`verdict-hardening` precedent).
- **Worktree:** `herdr worktree create` → `sm/maker-restart-handoff`.
- **Planner committee:** resolved by the fork's own direct plan-committee synthesis rather than a full `/adhd` divergent-ideation pass, given the fork's autonomous-execution time constraints; the research report itself already supplied the divergent exploration this task needed.
- **Rounds:** an unusually eventful task for what looked like a small, well-specified change — checker round 1 found 2 real, CONFIRMED gaps; the follow-up fix rounds then surfaced a repeated "commit message doesn't match its own diff" pattern that consumed most of this task's time:
  1. **(fork-found, pre-checker):** the SKILL.md exit-code documentation block for `loop-guard.sh action` had a factual self-contradiction — its own exit-0 bullet claimed exit 0 covers "n >= ABORT_REPEATS with hard-abort message," directly contradicting the exit-3 bullet three lines below and the fork's own independent 12-point mutation walk (n=1..12 at ABORT_REPEATS=10), which confirmed n>=ABORT_REPEATS always exits 3, never 0. A second, unrelated typo: README's `loop-guard.sh` table row ended with a stray, unbalanced closing paren. Both fixed by the maker in one round before the checker ever ran.
  2. **(checker round 1, real cross-model `gpt`, qa/coverage + qa/test-reality):** (a) the round-state.md WRITE instruction (the maker-facing "before replying DONE, write/update..." sentence) was only ever added to the 2 Claude-maker prompt sites — all 5 pi-maker prompt-assembly sites (initial herdr route, its headless fallback, and 3 fix-round loop-back sites) still had ONLY the read-injection, never the write instruction, meaning the whole P1 mechanism silently depended on the supervisor remembering to include it by hand for every pi-routed task — exactly the dependency this task exists to remove. (b) the new loop-guard.sh selfcheck only exercised the exit-5/exit-3 boundary at a single `ABORT_REPEATS=4` value (n=3,4), not the full `n=1..ABORT_REPEATS+1` range at multiple thresholds as the fork's own HAMMER instruction had explicitly asked for — a real regression-coverage gap, though the fork had already independently confirmed the underlying logic correct via direct execution.
  3. **(fork-found, after the maker's first attempted fix of #2):** the maker's own commit message claimed "checker fix #2: expand loop-guard.sh selfcheck with comprehensive exit-code testing," but the actual diff for that commit touched only `skills/secondmate/SKILL.md` — `bin/loop-guard.sh` was byte-identical to the prior commit. Separately, of the 7 total maker-launch prompt sites, only 6 had gained the write instruction from the round-2 fix — the fix-round loop-back "Claude maker" site (one of the two sites that HAD the write instruction from round 1, but whose separate fix-round duplicate never got it) was still missing it. Both caught by the fork directly grepping/diffing the actual file content rather than trusting the commit message.
  4. **(fork-found, second fix attempt):** the maker's SECOND commit's message again described exactly the requested loop-guard.sh selfcheck expansion, and the file DID change this time — but the maker had entered a genuine debugging spiral first (created and ran multiple scratch `track-*.sh`/`trace.log` files, burning context past 80%), and the fork independently traced the actual root cause itself before the maker converged: a bare, unguarded `action --key` call at the original selfcheck's line 66 (`ABORT_REPEATS=5`, n=3 check) ran under the file's own `set -euo pipefail` with no `|| true`/`set +e` guard, silently aborting the whole selfcheck script right there; separately, several boundary checks called `action --key same` an EXTRA time "to recheck the message," which — since `action` is stateful — silently advanced the counter an extra, unaccounted step, making every subsequent `n` label off-by-one from its own real counter value. The fork wrote a complete, minimal replacement selfcheck block itself (one call per `n`, walking `n=1..ABORT_REPEATS+1` for both `ABORT_REPEATS=5` and `10`), mutation-tested it against 2 known-bad reverts (an off-by-one boundary, a full revert to the original silent-gap behavior) confirming both were caught with specific, correct failure messages, then handed the exact verbatim block to the maker with the precise splice instructions. The maker's next commit's message correctly described this exact fix and root cause — but the diff only touched `skills/secondmate/SKILL.md` again (a genuinely separate, real fix — closing the last missing 7th write-instruction site) while `bin/loop-guard.sh` remained untouched, a THIRD instance of a commit message misdescribing its own diff in this one task.
  5. Given the repeated pattern and the maker's context already past 80%, the fork restarted the maker fresh on the same pane (herdr has no direct agent-kill primitive; used a fully mechanical, zero-judgment final prompt — write a self-contained Python splice script to a file inside the worktree, run it, verify `selfcheck` prints exactly `ok`, commit) rather than sending another abstract instruction into an already-confused context. This succeeded on the first attempt: the fork independently confirmed the committed file byte-identical to its own pre-verified template, re-ran the selfcheck (`ok`), and re-ran both mutation tests directly against the real committed file (both correctly caught).
  6. **Checker round 2: clean pass, zero findings.** Independently re-walked `n=1..ABORT_REPEATS+1` for `ABORT_REPEATS=3,4,5,10`; independently verified reset preserves unrelated files via a live fixture; independently confirmed exactly 7/7 read+write pairing by line number; confirmed no leftover scratch debris. One non-blocking SPEC AMBIGUITY noted, not chased: for `ABORT_REPEATS<=2`, the unconditional `n<3` silent-exit clause takes precedence over what would otherwise be an immediate hard-abort at `n=1` or `2` — an unrealistic config value (the only tested/documented value is the default 10), matching this repo's established precedent for narrow, impractical edge cases (accepted and documented, not fixed).
- **Merge-reconciliation:** first `merge-sequencer.sh` attempt hit a real `MERGE_CONFLICT` — main had advanced through the concurrently-merged `verdict-hardening` task (including its own version bump to `0.1.28`) since this branch's stale `1861eed` base. Resolved by merging `main` into the worktree branch directly (two trivial version-string conflicts in `.claude-plugin/plugin.json`/README's badge, resolved to `0.1.29`, one past main's `0.1.28`; `docs/ARCHITECTURE.md`/`skills/secondmate/SKILL.md` auto-merged cleanly), re-verifying with a fresh `verify-gate.sh` PASS and a live `loop-guard.sh selfcheck` run against the merge commit before retrying — which then succeeded.
- **Outcome:** verify-gate PASS at `798ce34` (pre-reconciliation) and again at `bedbc73` (post-reconciliation merge commit) → human hold `caa994ae` answered `merge` (genuine `AskUserQuestion`, not self-answered) → merged to main via `bin/merge-sequencer.sh` at `7aefd2d`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed.
- **Lesson:** this task logged 3 separate instances of a maker's commit message describing work its own diff did not contain — the most severe case (claiming a selfcheck expansion that never touched the file at all) would have shipped a completely unverified boundary-logic rewrite as "tested" had the fork trusted the message instead of independently diffing and running the actual file every single round. The recovery pattern that finally converged — the fork tracing the exact root cause itself, writing and mutation-testing a complete replacement block independently, then handing over a fully mechanical (script-write-and-run, not prose-and-paste) final instruction after the second false-completion claim — is the strongest version yet of this repo's established "give exact verbatim code after a stuck-loop recurrence" precedent: mechanical delegation (run this exact script) proved more reliable than even a precise prose instruction (paste this exact text in this exact place) once the maker's own context was already degraded.

## 2026-09-23 — kimi-k3-planner-swap: upgrade the plan-committee's Kimi planner slot from Kimi-K2-Thinking to Kimi K3

- **Trigger:** user asked to check whether Kimi K3 is available on Bedrock and where it could fit in secondmate (planning vs maker layer). Confirmed `moonshotai.kimi-k3` ACTIVE in us-east-1/us-west-2, reachable only via the cross-region inference profile `global.moonshotai.kimi-k3` (bare model id rejected — "on-demand throughput isn't supported"). Live-tested clean via pi (text + real tool-call). Grepped the repo: Kimi has only ever been used in the planning committee (one of 6 headless, no-tool planners), never as a maker or checker. Presented a 3-way scope choice via `AskUserQuestion`; human chose the lowest-risk option — swap the existing planner slot only, defer maker/checker adoption as separate future decisions.
- **Maker path:** pi (Qwen3-Coder-Next, medium thinking), dedicated worktree workspace `w3F`/`w3J` (recreated once — see below), root pane.
- **Worktree:** `herdr worktree create` → `sm/kimi-k3-planner-swap`.
- **Planner committee:** ran unconditionally (6 pi planners). Every planner independently converged on the same load-bearing risk: a `FAKE_DUAL_BAD` selfcheck fixture keys off the literal old model string `moonshot.kimi-k2-thinking` and would silently stop testing the intended dual-failure scenario if not updated in lockstep with the rename — flagged CRITICAL by deepseek-r1, called the "hardest step" by qwen3-coder, the top "rabbit hole" by qwen3-80b, a "silent test coverage erosion" risk by kimi-k2 itself, and an explicit probe by glm5. Noise discarded: hallucinated caching layers/dashboards/SLOs/cost-anomaly alerts (kimi-k2's own review, describing a distributed service that doesn't exist — this is a stateless bash script with no cache), a large security-theater section from mistral-large3 (SQL injection, IDOR, path-traversal via a "Read tool" the planners are explicitly denied), and two over-engineered alternatives from qwen3-80b (a model-ID abstraction layer, an AWS-alias request) — both discarded in favor of the flat-array direct-swap every other planner converged on. All probes answered directly from the repo (no cache/CI/dashboard exists to ask about); zero escalations.
- **Rounds:** 1 checker round (`gpt`, `qa/coverage` + `qa/test-reality` lenses), clean pass, zero findings, run in the strict visible pane (per this repo's own standing rule) via a script file (avoiding the known `herdr pane run` quoting bug). The checker independently re-derived and confirmed: all 6 PLANNERS entries correct, `_prompt_kimi_k3`/case-branch routing correct, `FAKE_DUAL_BAD`'s model-match + retry-count + `.md.healed` marker rename all consistent together, doc/version sync correct (`0.1.25` everywhere), scope contract clean (`bin/committee-output.py` byte-identical object ID, zero unrelated files touched). One SPEC AMBIGUITY noted and correctly self-resolved: a historical `kimi-k2` reference in this very file's own prior entries is intentionally NOT rewritten (it describes a past incident, not current state) — the checker treated the scoped README/SKILL/ARCHITECTURE requirement as controlling, matching the maker's actual (correct) choice.
- **Real blocker found AFTER the maker's work was done but BEFORE the checker ever ran (supervisor's own live end-to-end test, not a synthetic fixture):** `pi`'s remote model catalog (fetched from `pi.dev`, persisted in `~/.pi/agent/models-store.json`) reported `maxTokens: 131072` for `global.moonshotai.kimi-k3`/`us.moonshotai.kimi-k3` — exceeding AWS Bedrock's actual enforced `128000` ceiling — causing every real call to fail regardless of `--thinking` level or inference-profile form. Root-caused by downloading and reading `pi`'s own npm packages (`@earendil-works/pi-ai`, `@earendil-works/pi-coding-agent`) directly: the bundled catalog has no Kimi K3 entry at all; the value came from `core/remote-catalog-provider.js`'s `pi.dev` overlay, confirmed by inspecting the raw cache file. Fixed durably via `~/.pi/agent/models.json` (`modelOverrides`), a genuine topmost user-config layer per `provider-composer.js`'s own source comment — verified NOT a fragile cache hack (survives future remote-catalog refreshes). A `pi update` (0.84.4→0.87.1) run mid-investigation did not fix the catalog value and was a red herring that initially looked like it also broke `deepseek-r1` (a completely unrelated, already-in-production planner) — that turned out to be a **self-inflicted testing artifact**: the real script (`bin/plan-committee.sh`) already always passes `--no-tools` to every planner invocation (confirmed by reading the actual invocation lines), but the supervisor's own manual raw `pi` reproduction calls during debugging omitted that flag, hitting a genuine but irrelevant Bedrock constraint ("this model doesn't support tool use in streaming mode") that the real production script never actually encounters. Caught only by re-running the REAL script end-to-end a second time and seeing `deepseek-r1` succeed cleanly with no code or config change beyond the Kimi fix — a direct instance of this repo's own recurring lesson (test the real path, not a hand-rolled reproduction) applied to a debugging session, not just a code change.
- **Process pause (genuine, not self-resolved):** work stopped before the checker ever ran once the live-call blocker was found — merging would have made the mandatory, unconditional plan-committee step hard-fail on every future task. Reported fully via `AskUserQuestion` rather than assumed; human chose to hold the branch (keep Kimi-K2-Thinking on `main`) rather than chase a pi-version pin. The worktree was torn down (branch kept, per "park" not "abandon") while blocked, then **recreated cleanly from the existing branch** once the fix was confirmed (`herdr worktree create --branch <existing-branch>` checks it out rather than erroring or duplicating — confirmed empirically, not documented anywhere prior to this task) and the loop resumed exactly where it paused.
- **Outcome:** verify-gate PASS at `80779a3` (checked-sha match) → human hold `c62b7c28` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` — first attempt hit `PUSH_FAILED` (origin had genuinely diverged: an unrelated, concurrent `chore: update clone count` auto-commit from the weekly cron workflow landed on `origin/main` between fetch and push; merge-sequencer's own documented behavior — the local merge commit is NOT reverted on a push failure) → `git fetch` + a clean, non-conflicting `git merge origin/main` (auto-merged `README.md`'s cache-buster query param) → `git push origin main` succeeded at `6b3a4ab` → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual final pushed commit → `claude plugin validate .` passed → worktree/branch torn down (no marker existed — this task never ran a maker session against the recreated worktree, only checker/gate/hold, so no scope-guard marking was ever needed for the resume phase).
- **Deferred, tracked for follow-up:** the actual root-cause fix (`~/.pi/agent/models.json`) lives outside this git repo, on this one machine — nothing in secondmate detects or repairs its absence on a fresh machine/session. Human chose (via a second `AskUserQuestion` in the same turn) to add a `bin/doctor.sh` check for this, as a separate follow-up task — not built in this task.
- **Lesson:** the single biggest time sink this task was the supervisor's own debugging methodology, not the underlying bug — manually reproducing a `pi` call by hand (correctly, for isolating the Kimi issue) but omitting a flag (`--no-tools`) that the REAL script always includes, produced a plausible-looking but entirely fictitious second "production regression" in an unrelated model that took real npm-package archaeology to chase before the real script's own source settled it in one grep. When a manual reproduction diverges from a suspected regression's real invocation site, re-check the real invocation's exact argv before trusting the manual repro's symptom as evidence of anything beyond what it actually tested.

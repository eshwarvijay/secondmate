## 2026-09-17 — maker-permission-mode-auto: Claude maker launch recipe --permission-mode acceptEdits → auto

- **Trigger:** direct human instruction — Opus is temporarily unavailable, so Claude Sonnet 5 is substituting as the maker model, but the maker was still launching with `--permission-mode acceptEdits`; the human wants `auto`, the value previously used when Opus was the maker.
- **Planning:** full 6-model plan-committee run (unconditional convention), adhd skipped (mechanical, closed-ended, exact value given by the human). All probes resolved with no escalation: `claude --help` confirmed `auto` is a real, valid `--permission-mode` choice (not a typo); grep confirmed exactly 2 occurrences of the target line in `skills/secondmate/SKILL.md` and zero occurrences of `acceptEdits` anywhere else in the repo; `bin/session-activate.sh`'s supervisor-invariants text doesn't specify a `--permission-mode` value at all, so no change needed there. One planner (mistral-large3) returned garbled, tool-call-shaped text (`[TOOL_CALLS]<prompt>...`) mid-response — discarded as noise, flagged as possibly worth a future look at whether it's a new bypass of the tool-call detector built in the earlier `fix-plan-committee-tool-call` task, not chased further here.
- **Maker path:** pi (Qwen3-Coder-Next, medium thinking), dedicated worktree workspace `w2P`, root pane `w2P:p1`.
- **Worktree:** `herdr worktree create` → `sm/maker-permission-mode-auto`.
- **Rounds:** 1 checker round, clean pass, zero findings — a genuinely trivial, fully-scoped two-word change (both Claude maker launch recipes: the headless-fallback section and the "Maker pane" visible-orchestration section) with no room for ambiguity once the plan-committee's probes were resolved.
- **Outcome:** verify-gate PASS at `f37065b` (checked-sha match) → human hold `4a0e6b41` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `18a516e`, pushed → version bump (`0.1.19` → `0.1.20`, done correctly THIS time before the selfcheck chain, not after) → full repo-wide selfcheck chain re-run clean (`ALL_OK`) → `claude plugin validate .` passed → worktree/branch/workspace/marker torn down.

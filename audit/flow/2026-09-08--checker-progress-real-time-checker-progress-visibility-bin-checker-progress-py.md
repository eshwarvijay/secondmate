## 2026-09-08 — checker-progress: real-time checker progress visibility (bin/checker-progress.py)

- **Trigger:** user request — checker rounds ran silently for their full duration with zero intermediate output; user wanted to see what the checker is doing while it runs. adhd was interrupted (user opted to skip ideation ceremony for this task); user also explicitly ruled out an interactive/visible-pane checker mode after a pros/cons discussion, keeping headless-only and pushing progress into the script itself (`pi --mode json` piped through a new filter, `--mode text`-parity preserved on stdout for `verdict.py`).
- **Maker path:** pi (Qwen3-Coder-Next), dedicated worktree workspace `w1M`, root pane `w1M:p1`
- **Worktree:** `herdr worktree create` → `sm/checker-progress`
- **Rounds:** 4 checker rounds, each found and fixed a real, confirmed bug: (1) `stopReason` `error`/`aborted` not suppressing output, only the first text-part extracted instead of all parts, `err_file` param silently ignored (masked by the selfcheck's own stderr-patching trick — caught via a direct unit call bypassing it); (2) caller-supplied `--mode text` in `launch-checker.sh`'s documented passthrough interface silently overriding the injected `--mode json` (pi takes the LAST `--mode` occurrence) causing total silent failure — fixed by moving the injected flag structurally last in the exec line; empty text parts filtered out during multi-part concatenation, deviating from real `--mode text` behavior; (3) a single empty text-type content part produced 0 stdout bytes instead of the required single newline (`if final_text:` truthiness check treating empty-but-present as absent); README/ARCHITECTURE docs missing the new script; (4) clean pass — checker traced the empty-text-part and no-assistant-message edge cases by hand per the round's addendum and found nothing new.
- **Process correction (mid-task, repeated user pushback):** rounds 1-2 were run by the supervisor as blocking calls directly in its own shell — this both defeated the point of the progress feature (the supervisor's own turn sat blocked) and violated this repo's own documented "Checker pane" recipe (checker must run inside the dedicated worktree's own pane, never the supervisor's). Corrected from round 3 onward: split a pane off the worktree's own root pane (`herdr-pane.sh split --pane <root_pane_id> --dir down`), ran `launch-checker.sh` there via `herdr pane run` with **no output redirection** (redirecting to files was tried once and defeats visibility entirely), then read the pane's scrollback afterward for `verdict.py` parsing.
- **Outcome:** verify-gate PASS at `564c017` (checked-sha match) → human hold answered `merge` (genuine per-task approval) → merged to main `--no-ff` at `0501e25` → full repo-wide selfcheck chain re-run clean on the merged commit → pushed → worktree/branch/workspace torn down.
- **Lesson:** `herdr agent prompt --wait` does not reliably block until the agent is actually done working (observed returning `agent_status: done` once while the pane's own terminal still showed `⠴ Working...` seconds later on an unrelated concurrent task) — `herdr agent wait <target> --until idle --until done --until blocked` is the correct primitive for a real blocking wait on agent state.

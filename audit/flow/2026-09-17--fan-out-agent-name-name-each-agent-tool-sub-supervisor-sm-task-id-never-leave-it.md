## 2026-09-17 — fan-out-agent-name: name each Agent-tool sub-supervisor sm-<task-id>, never leave it unset

- **Trigger:** direct human observation, live during this exact session — the two sub-supervisor Agent-tool spawns tested earlier (`fix-doctor-status-line`, `fix-scope-guard-marker-doc`) showed up under a generic, indistinguishable label (just the `subagent_type`, "general-purpose") because the supervisor never passed the Agent tool's own `name` parameter. Confirmed directly this session: `name` is a real, accepted Agent-tool parameter distinct from `subagent_type`, and `SendMessage` uses that same name as its `to` field to resume a previously-spawned agent later.
- **Planning:** full 6-model plan-committee run, adhd skipped (mechanical, closed-ended, exact fix already known). One genuine ambiguity surfaced and resolved during synthesis (glm5): should the outer, dispatcher-level sub-supervisor name differentiate by maker type (`sm-pi-<task-id>` vs `sm-<task-id>`), matching step (b)'s existing inner-maker convention? Resolved: no — the OUTER Agent-tool spawn happens before any maker-routing decision even exists (that's decided LATER, inside the sub-supervisor's own execution), so it should always be plain `sm-<task-id>`, explicitly distinguished in the new text from step (b)'s separate, deeper-nested inner-maker naming. Noise discarded: an entire security-theater analysis from one planner (entropy requirements, name-collision registries, sanitization pseudocode) for what is a documentation sentence about the supervisor's own single-session tool usage, not a multi-tenant adversarial system; a "dynamic naming service" option, over-engineered for a 3-sentence doc fix.
- **Maker path:** pi (Qwen3-Coder-Next, medium thinking), dedicated worktree workspace `w2Y`, root pane `w2Y:p1`.
- **Worktree:** `herdr worktree create` → `sm/fan-out-agent-name`.
- **Rounds:** 2 checker rounds, the first a self-inflicted false alarm the supervisor caused and then corrected, not a maker defect: round 1's checker correctly found that the shipped addition was three sentences where the supervisor's own task description had said "one sentence" — but the supervisor's own consolidated plan (written before the maker was ever involved) had always specified exactly this three-sentence content, and each sentence carries genuinely distinct, necessary information (the instruction itself; the outer-vs-inner naming resolution a planner had explicitly flagged; the why-it-matters tie to `SendMessage` resume). Adjudicated as a wording imprecision in the ask, not a scope violation in the delivery — re-ran round 2 with corrected framing asking the checker to judge the content on correctness/completeness rather than a literal sentence count, which returned a clean pass confirming all three sentences are non-redundant.
- **Outcome:** verify-gate PASS at `d2d9d00` (checked-sha match) → human hold `61d7d274` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `2ec0fe2`, pushed → version bump (`0.1.21` → `0.1.22`) → full repo-wide selfcheck chain re-run clean (`ALL_OK`) → `claude plugin validate .` passed → worktree/branch/workspace/marker torn down.
- **Lesson:** a checker correctly flagging a literal mismatch between a stated constraint and the actual delivered content is not automatically evidence the content is wrong — it can just as easily mean the constraint itself was mis-stated by whoever wrote the task. The right response in that case is for the supervisor to re-examine its OWN framing before routing anything back to the maker, and if the content is independently verified correct, to re-run the checker with corrected framing rather than degrade good, necessary content just to satisfy an inaccurate literal count.

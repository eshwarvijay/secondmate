## 2026-09-24 — verdict-hardening: file:line-enforced findings validation + --lenses cross-check/ledger for bin/verdict.py

- **Trigger:** cloudflare/security-audit-skill patterns discovered via starred-repo research earlier this session, adapted into two hardening changes for secondmate's own checker-verdict contract: (1) `fail` findings must carry a real `file:line` token or the explicit `[NOLOC]` escape hatch, rejecting empty/non-string findings; (2) a new `--lenses <comma-list>` flag cross-checks that every lens injected via `launch-checker.sh --lens` was reported as covered in the envelope's additive `lens_coverage` field, failing closed (`ambiguous`, exit 2) on any gap, and records every `--lenses` invocation to a `git-common-dir`-anchored `audit/lens-coverage.jsonl` ledger (same `fcntl` idiom as `hold.py`/`claim-ledger.py`).
- **Maker path:** `deepseek.v3.2` via `herdr agent start --kind pi`, dedicated worktree workspace `w3Q`, root pane `w3Q:p1`. Ownership transferred mid-task from the coordinator to a fork given full autonomy through merge/push/teardown (matching the `lesson-store` task's own precedent the same day).
- **Worktree:** `herdr worktree create` → `sm/verdict-hardening`.
- **Rounds:** 8 real checker rounds total (this entry covers rounds 6-8, run by the fork; rounds 1-5, run before the handoff, fixed envelope last-object-wins selection, missing `--lenses` in the documented recipe, empty-string `--lenses` skipping the ledger, no ledger-failure-path test, a truthy-vs-`is True` `lens_coverage` gate, an undocumented ledger env var, a non-hermetic selfcheck, and — independently discovered via a real cross-task side effect, not a checker round — a genuine cross-task `audit/lens-coverage.jsonl` collision from a bare relative-path default, closed by anchoring via `git rev-parse --git-common-dir` matching `claim-ledger.py`'s exact precedent). Rounds 6-8, all fixed directly by the fork rather than routed back to the maker (see reliability note below):
  1. **(round 5→6):** a URL host:port (`https://bugs.example:443`) was accepted as a fake `file:line` location — the `[^\s:]+` group's match starts at the URL's own `//`, which no real relative path ever does. Fixed with a `_finding_has_location()` helper rejecting matches starting with `//`.
  2. **(round 6, checker):** bare numeric colon-patterns (a timestamp `10:30`, a ratio `16:9`) also passed — the third round on the same "what looks like a path" boundary. Generalized rather than patched again, per this repo's own named `fix-plan-committee-tool-call` precedent: require the matched candidate contain at least one letter, since no real file path is purely numeric. Deliberately left open and documented: a domain/email-shaped string with letters and a port (`user@example.com:25`) still passes this shape-only heuristic.
  3. **(round 7, checker):** the letter check used an ASCII-only `[A-Za-z]` regex, so a real file with a non-ASCII name (e.g. a Greek-letter-only filename, `λ:12`) was wrongly rejected as a bare numeric pattern — the exact inverse failure mode of finding #2, caught because round 7's checker was explicitly asked to keep probing the same boundary. Fixed with `str.isalpha()` (Unicode-aware). The fork's own first draft of the regression assertion used a fixture with a `.py` extension (`λ.py:12`), which still contains ASCII letters and would have passed even the old broken regex — caught by mutation-testing the assertion itself (revert the fix, confirm the assertion does NOT fail, recognize the fixture is wrong, fix the fixture to be ASCII-letter-free, re-mutate, confirm it then genuinely fails) before trusting it.
  4. **Round 8: clean pass, zero findings**, after independently reproducing the Unicode fix, its mutation test, and the vacuous-fixture near-miss, and probing further Unicode edge cases (fullwidth digits, Roman numeral glyphs, a lone combining mark) without finding a new bypass.
- **Outcome:** verify-gate PASS at `97782f8` (checked-sha match) → human hold `497f1957` answered `merge` (genuine `AskUserQuestion`, not self-answered) → version bumped `0.1.25`→`0.1.28` (one past main's `0.1.27`) and a pre-existing, unrelated doc-sync gap fixed in the same commit (`bin/launch-checker.sh --selfcheck` had a `--selfcheck` flag but was never in README's manual chain) → first `merge-sequencer.sh` attempt refused on a real dirty-primary-checkout state (`audit/lens-coverage.jsonl`/`.lock`, the new ledger's own default path, correctly and intentionally anchored at the shared git-common-dir per the claim-ledger precedent, but never gitignored) → fixed with a `.gitignore` addition directly on main (`5050c72`), the same class of gap already hit for `.secondmate/`, `audit/merge-ledger.jsonl`, and `audit/metrics.jsonl` → second attempt hit a real `MERGE_CONFLICT` against the concurrently-merged `lesson-store` task (both branches bumped `plugin.json`/README's version badge independently) → resolved by merging `main` into the worktree branch (kept `0.1.28`, already correctly one past main's `0.1.27`; kept both branches' README selfcheck-chain additions — `launch-checker.sh --selfcheck` and `lesson-lookup.py selfcheck`; `docs/ARCHITECTURE.md`/`SKILL.md` auto-merged cleanly) → re-verified with verify-gate and a live selfcheck run before retrying → merged to main via `bin/merge-sequencer.sh` at `67942da`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed.
- **Maker reliability note:** `deepseek.v3.2` was severely unreliable across this task's full 8 rounds — failed to commit real, correct work across 4 separate rounds (caught each time via direct `git status`, not a completion claim), replied DONE without applying a requested one-line fix across one full round, and once degenerated into a genuine model-pathology infinite-token-repetition loop (`<｜DSML｜function_calls` repeated thousands of times) burning real cost with zero progress. Given the fork's own constraint against pausing mid-task to ask a human, and two consecutive rounds of zero progress from a demonstrably pathological process, the fork made the judgment call to implement rounds 6-8's fixes directly rather than risk a further costly maker round — each fix still went through the full independent-reproduction, checker-review, and mutation-testing discipline used throughout the rest of this task, just without an intervening maker round.

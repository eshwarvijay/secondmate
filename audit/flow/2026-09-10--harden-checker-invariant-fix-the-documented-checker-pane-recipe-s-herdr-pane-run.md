## 2026-09-10 — harden-checker-invariant: fix the documented checker-pane recipe's herdr pane run quoting bug + add a standing SessionStart invariant

- **Trigger:** direct follow-up to the user's explicit choice ("Both: retroactive checker + harden the rule") after catching the supervisor skipping the real cross-model checker for 2 rounds on the `claim-ledger`/`merge-sequencer` tasks. This task closes the second half of that commitment: the standing "split a pane off the worktree's root pane, `herdr pane run` the checker" recipe in `skills/secondmate/SKILL.md` still documented the OLD, broken inline-multi-command form (`herdr pane run "$ck" "cmd1 ; cmd2 ; echo MARKER"`) discovered and worked around ad-hoc earlier this session (see `hold-integrity-hardening`/`fix-plan-committee-tool-call` entries) — the workaround (write the full invocation to a script file, `herdr pane run <pane> bash <script-path>`) had never been folded back into the actual documented recipe or into a standing invariant, so a future session could silently regress to the broken pattern.
- **Planning:** full 6-model plan-committee run (per this repo's unconditional-committee convention), adhd skipped (mechanical, closed-ended, known root cause per global CLAUDE.md's own gate). Planners mostly converged on "fix SKILL.md recipe + add a terse invariant to `bin/session-activate.sh`," but several over-scoped it (temp-file cleanup/rotation schemes, a runtime `assert_single_argv` guard, injection/path-traversal threat modeling) — all extracted as noise and discarded during synthesis: `session-activate.sh` only fires once at SessionStart and prints static text, so a runtime enforcement guard is structurally impossible there (it can't intercept a later tool call the way `scope-guard.py`'s PreToolUse hook can); the script path/content in this pattern is always supervisor-authored literal text, never untrusted input, so no new injection surface exists.
- **Maker path:** pi (Qwen3-Coder-Next, medium thinking), dedicated worktree workspace `w21`, root pane `w21:p1`.
- **Worktree:** `herdr worktree create` → `sm/harden-checker-invariant`.
- **Rounds:** 1 real checker round (clean pass, zero findings) — but only after the supervisor caught and fixed a genuine bug in the maker's FIRST commit before ever engaging the checker: the maker's SKILL.md fix wrote the checker script via a heredoc with a QUOTED delimiter (`cat > script.sh << 'SCRIPT_EOF'`), which disables shell variable expansion inside the heredoc body — `${CLAUDE_PLUGIN_ROOT}` would have been written to the generated script LITERALLY, unexpanded, silently reintroducing a different broken-recipe bug in the exact fix meant to eliminate one. Caught by the supervisor independently reproducing the heredoc-quoting behavior with a minimal standalone test (`cat > f << 'EOF' / ${X}/foo / EOF` → literal `${X}/foo`; unquoted `<< EOF` → correctly expanded) before ever routing anything to the checker. Routed back to the maker as a targeted one-line fix (unquote the delimiter); the maker's second commit switched to `<< SCRIPT_EOF` (unquoted) — independently re-verified by the supervisor with the same test harness, confirming `${CLAUDE_PLUGIN_ROOT}` now expands at write time while the intentionally backslash-escaped `\$?` correctly stays a literal `$?` for the generated script to evaluate at its own runtime. The subsequent real checker round found nothing new — it independently re-derived and confirmed the exact same two facts (heredoc expansion correctness, scope/patch integrity) via its own from-scratch mock-checker repro before returning `pass`.
- **Outcome:** verify-gate PASS at `90eb516` (checked-sha match) → human hold `148f20fb` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` itself (its first real production use, not just its own selfcheck) at `be4b0b0`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed → worktree/branch/workspace/stale marker torn down.
- **Process note:** mid-task, the human asked why no checker pane was visible on screen — clarified that `herdr worktree create` and pane `split` both use `--no-focus` by design (so orchestration never steals the human's focus), and that the checker pane (`w21:p2`) had already been read and closed per the standard "clean up only the panes you created" step by the time the question was asked. No process gap here, just a visibility/timing question — resolved by explaining the actual pane lifecycle rather than by changing behavior.
- **Lesson:** this is the first task in this repo's history where the supervisor caught a maker's bug through direct reproduction BEFORE any checker round ever ran, entirely avoiding a "checker round 1: fail" cycle — worth normalizing: when a fix's correctness hinges on a mechanically verifiable, cheap-to-reproduce shell/quoting fact (heredoc delimiter quoting, escaping rules), reproduce it directly with a throwaway test the moment the diff is read, rather than waiting for the checker to (eventually) find the same thing.

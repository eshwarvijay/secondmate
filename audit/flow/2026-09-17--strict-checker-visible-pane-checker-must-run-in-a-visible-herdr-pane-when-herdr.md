## 2026-09-17 — strict-checker-visible-pane: checker must run in a visible herdr pane when HERDR_ENV=1, no headless exceptions

- **Trigger:** direct human instruction ("checker should be in the visible side pane instead of the headless, as a strict rule"), given mid-turn while the supervisor was still finishing the prior task's housekeeping. Root-caused before scoping: step 4 ("Check") in `skills/secondmate/SKILL.md` — the FIRST place a supervisor encounters checker instructions — showed a plain headless `launch-checker.sh` invocation with zero mention of `HERDR_ENV` or the visible-pane requirement, appearing ~170 lines BEFORE the "Visible orchestration in herdr" section that documents the actual visible "Checker pane" recipe. This exact gap is already documented in this repo's own `audit/decision.md` as a real incident this same session ("the supervisor initially self-answered this hold without genuine approval... ran checker rounds 1-2 as blocking calls in its own shell instead of inside the dedicated worktree's own pane" — the `checker-progress`/`claim-ledger` tasks).
- **Planning:** full 6-model plan-committee run, adhd skipped (mechanical once root-caused, exact wording largely pre-resolved by the supervisor before handoff). Noise discarded: a runtime-enforcement wrapper/pre-commit-hook idea (explicitly out of scope — no bin/ script logic changes); template-injection/markdown-safety concerns from one planner (inapplicable — no template engine renders these files); ALL-CAPS formatting suggestion (rejected in favor of this file's own existing bold-inline-text convention, e.g. the pre-existing `**IMPORTANT:**` pattern).
- **Maker path:** pi (Qwen3-Coder-Next, medium thinking), dedicated worktree workspace `w2Q`, root pane `w2Q:p1` (agent named `sm-pi-checker-visible-pane` — the natural `sm-pi-strict-checker-visible-pane` exceeded herdr's 32-character agent-name limit by one character, shortened without changing the task-id/branch/worktree naming).
- **Worktree:** `herdr worktree create` → `sm/strict-checker-visible-pane`.
- **Rounds:** 1 checker round after 1 maker-side self-correction — the maker made all three specified edits correctly (verified byte-for-byte against the supervisor's own verbatim spec) but never committed, the exact "known failure pattern" this repo's own maker prompts have warned about since the `lessons-checklist` task; caught directly via `git status` (not trusted from any completion claim), fixed with a commit-only nudge. The subsequent real checker round returned a clean pass, zero findings — including independently byte-comparing the actual "Checker pane" recipe code block against its pre-change version (1,072 bytes, identical) and running `session-activate.sh` with `HERDR_ENV` both unset and `=1` to confirm the emitted invariant text is correct and static in either case.
- **Outcome:** verify-gate PASS at `5de3315` (checked-sha match) → human hold `3402a9ca` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `7e9334a`, pushed → version bump (`0.1.20` → `0.1.21`) → full repo-wide selfcheck chain re-run clean (`ALL_OK`) → `claude plugin validate .` passed → worktree/branch/workspace/marker torn down.
- **Lesson:** the supervisor's own compliance with this exact rule throughout the task (dispatching the checker via the visible pane, as always this session) meant the fix's own verification process was itself a live demonstration of the rule working as intended — worth noting since a rule about process discipline is more convincing when the task that ships it is visibly following it.

## 2026-09-10 — dispatch-loop: fan-out pattern for 2 concurrent sub-supervisors + bin/dispatch-report.py (3rd of 3 dispatcher primitives)

- **Trigger:** direct continuation of the explicit "build all these with each as a task" instruction (claim-ledger + merge-sequencer already merged earlier this session) — this is the third and final primitive.
- **Planning:** full ADHD divergent-ideation pass (5 parallel frames: remove-load-bearing-assumption, 3am-on-call, logistics, ant-colony, inversion — 30 raw ideas) followed by a Phase-2 focus/deepen pass (3 parallel deepen agents on the top 3 clusters: spawn-and-track primitive, liveness/reaping diagnostic, safe-default failure semantics), then the standard 6-model plan-committee.sh. ADHD's own convergence was the key finding: two independent frames (remove-assumption and inversion) arrived unprompted at the identical conclusion that the third primitive can't be a scripted control loop at all — the judgment work (triage, routing, verdict adjudication, human holds) is LLM-driven, so at most this primitive is a documented spawn-and-track pattern plus a tiny deterministic completion-tag parser, never a new "engine." Traps explicitly named and discarded: ant-colony pheromone/quorum-sensing mechanisms (solve decentralization-at-scale problems this small, centrally-coordinated, human-supervised system doesn't have), worktree/pane "backhaul" reuse across tasks (directly conflicts with the established one-worktree-one-task scope-guard invariant), a redundant "waybill" ledger duplicating claim-ledger's own custody record.
- **Human scope decision (via AskUserQuestion, after the ADHD synthesis):** of the 3 ADHD-converged pieces (spawn-and-track pattern, liveness/reaping diagnostic, safe-default dwell/staleness sweep), only the first was built in this task — the other two are separate, deferred future tasks, mirroring claim-ledger.py's own precedent of deliberately punting liveness to a future caller. Also decided: hard-cap concurrent sub-supervisors at N=2 (not a tunable), specifically because every ADHD-deepened branch converged on the same real ceiling — human hold-answering attention doesn't scale with agent count, and this repo had already self-caught itself rubber-stamping a hold once under single-task load (`always-plan-committee`, 2026-09-04).
- **Maker path:** pi + Claude Sonnet 5 (`global.anthropic.claude-sonnet-5`, high thinking), dedicated worktree workspace `w23`, root pane `w23:p1`.
- **Worktree:** `herdr worktree create` → `sm/dispatch-loop`.
- **Rounds:** 3 real checker rounds (`gpt`, genuine cross-vendor pair), each finding real, confirmed bugs:
  1. **(supervisor-found, before any checker round):** no `.claude-plugin/plugin.json` version bump / README badge sync on the maker's first commit — a gap versus this repo's own standing per-merge convention (established across every prior task this session), not caught by the plan or the maker. Routed back and fixed (`0.1.16` → `0.1.17`) before the checker ever ran.
  2. **(checker round 1, most severe finding of the task):** the new SKILL.md fan-out section's steps were ordered so the sub-supervisor called `bin/merge-sequencer.sh` (step d) BEFORE opening its `bin/hold.py` entry for human approval (step e) — read literally, a sub-supervisor could merge code before a human ever approved it, inverting the existing single-task loop's own established Gate → Hold → Integrate contract (the same ordering shown in this repo's own README mermaid diagram). Independently confirmed by the supervisor via direct diff inspection (not just trusting the checker's `[SUSPECTED]` tag) before routing the fix. A second, unrelated round-1 finding (`SM_DONE_MERGED:not-a-sha` accepted with exit 0) was adjudicated as NOT a bug — the payload after the colon is deliberately opaque/unvalidated by design, mirroring `bin/verdict.py`'s own established philosophy of trusting the structural envelope rather than deeply validating payload semantics; `dispatch-report.py` was left unchanged in response.
  3. **(checker round 2):** the exact same merge-before-hold ordering bug, independently duplicated in `docs/ARCHITECTURE.md`'s own prose describing the same fan-out steps — the round-1→2 fix only touched SKILL.md, and this second file's summary of the same sequence was never updated to match. Confirmed by direct read, fixed the same way. Same round, a second confirmed bug: SKILL.md's corrected step (d) example command for `bin/hold.py hold` was missing hold.py's two REQUIRED arguments (`--task`, `--q`) — supervisor independently ran `python3 bin/hold.py hold --help` to confirm the real usage, and found the SAME file already showed the correct full form elsewhere (line 244) that the new section's example simply didn't match. A third round-2 finding (no automated regression test for the SKILL.md prose-ordering fix) was adjudicated NOT actionable — pure markdown documentation describing an LLM-driven SOP has no executable code path to assert against; checker review is the appropriate control here, matching the `harden-checker-invariant` task's identical precedent for a documentation-only fix.
  4. **Checker round 3: clean pass, zero findings**, after being explicitly asked to cross-check every other example command in both changed files against each referenced script's real `--help`/argparse output, the same way the round-2 `hold.py` bug was found — none found.
- **Supervisor verification discipline:** every fix was independently reproduced before being routed to the maker — ran `bin/dispatch-report.py selfcheck` directly and piped all 4 representative inputs through it to confirm exit codes 0/1/2/3 myself; mutation-tested the start-of-line anchor (removed `^` from the regex in a scratch copy, confirmed the mid-prose-embedded-tag selfcheck assertion then genuinely raised `AssertionError`, proving that test is real and not vacuous); grepped `claim-ledger.py`'s and `hold.py`'s actual argparse definitions to confirm every SKILL.md/ARCHITECTURE.md example command uses real flag names, not invented ones, before either trusting the maker's prose or accepting the checker's findings as correct.
- **Outcome:** verify-gate PASS at `9c61307` (checked-sha match) → human hold `857f8188` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` (its second real production use) at `31c519a`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed → worktree/branch/workspace/marker torn down.
- **Side incident (not a bug in this task's own deliverable):** discovered mid-teardown that this session's batch-scoped `caffeinate-guard.sh` process (started at the beginning of this session's task batch) had died — its 8-hour TTL had elapsed during an unusually long session spanning ADHD ideation, a 6-model plan committee, and 3 real checker rounds across this task alone, on top of everything already done earlier in the same session. Restarted immediately (`caffeinate-guard.sh start`, fresh 8-hour TTL) since teardown work was still in progress; not a defect in the script (it worked exactly as designed — a bounded ceiling, not a permanent guarantee), just a real-world case of a very long single session outrunning the ceiling.
- **Lesson:** this task's most severe finding (merge-before-human-approval ordering) and its most subtle one (a duplicate of that same bug hiding in a second file's prose summary) are both instances of a now-familiar failure class in this repo's history — a fix applied in exactly the place it was found, without checking whether the same underlying fact was also asserted somewhere else. The checker was directly asked to hunt for further duplicates in round 3 (a general sweep of every example command against real script help output) and found none — but the pattern itself (grep for every OTHER place a just-fixed fact might be restated, don't assume one file's fix closes the whole gap) is worth carrying into every future documentation-touching task.

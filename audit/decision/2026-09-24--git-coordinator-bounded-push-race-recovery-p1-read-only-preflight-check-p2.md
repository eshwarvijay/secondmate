## 2026-09-24 — git-coordinator: bounded push-race recovery (P1) + read-only preflight check (P2)

- **Maker (pi/Qwen3-Coder-Next):** implemented P1 (a bounded, race-signature-classified push-retry loop) and P2 (`--preflight-only` via `git merge-tree --write-tree`). The maker's own Test 19/20 fixture design was self-contradictory (a fake-git-shim meant to simulate a persistent race via a pre-receive hook, directly violating Test 20's own "hook rejections are never a race" requirement) and never converged despite one precise, targeted supervisor instruction — the supervisor took over the fix directly (a forked-worker execution context, continuing on the coordinator's own explicit directive, not the usual delegate-and-review pattern).
- **Decisions across 15 checker rounds (all supervisor calls; full technical detail in `flow.md`):**
  1. Chose bash-native `[[ =~ ]]`/`[[ == * ]]` matching over any pipe-based `grep -q` for every classifier, after discovering the SIGPIPE-under-pipefail bug class recurred at 3 different string sizes before fully closing.
  2. Chose to check hook-rejection signals with ABSOLUTE priority over race signals at every classification point (initial push AND every retry), rather than trying to enumerate "safe" keywords.
  3. When a structural fix for one classifier (`_is_ref_lock_race`, round 4) later turned out to need the identical fix for a SECOND, independent classifier (`_is_race`, round 13) — generalized the same way rather than adding a narrower, classifier-specific patch.
  4. **Round 9's own accepted-limitation writeup was WRONG** ("re-sampling per-retry would reopen round 8's self-deleting-hook fix") — the checker demonstrated this reasoning was flawed in round 10 (monotonic OR-sampling closes both directions simultaneously); the supervisor accepted the correction and fixed the reasoning, not just the code, updating all 3 places (script header, README, ARCHITECTURE) that had repeated the wrong claim.
  5. **Round 14's finding (a PATH-precedence fake-`git` shim can fabricate every structural marker this file relies on) was explicitly escalated to the human via `AskUserQuestion`** rather than judged unilaterally — this is a categorically different threat (PATH/binary-substitution, not a legitimate git mechanism) from every one of the prior 13 rounds' findings, and matches an already-accepted precedent elsewhere in this codebase (`bin/caffeinate-guard.sh`'s own documented PATH/supply-chain limitation) closely enough that the supervisor judged it worth a real decision point rather than either quietly accepting it or quietly chasing it. The human chose to accept it as documented, out-of-scope, citing that exact precedent.
- **Checker findings resolved, in chronological/severity order (14 real, 1 clean pass; full detail in `flow.md`):** SIGPIPE-under-pipefail (3 escalating fixes); hook-priority-ordering (found and fixed as 3 independent, escalating variants: initial-classification priority, free-text-collision spoofing, then a structural line-anchoring fix); an off-by-one push-attempt cap; two rounds of undocumented `--preflight-only` side effects (a ref-tracking fetch, an object-database write); a retry-loop escalation gap that let an unrelated failure (auth/network) burn a pointless extra attempt; the MOST SEVERE finding — the singleton lock released mid-recovery, defeating the script's entire serialization purpose, reproduced with two real concurrent invocations; a local `pre-push` hook defeating BOTH classifiers via its total lack of "remote: " framing (2 sibling instances, found and fixed together); a TOCTOU on that exact fix (a self-deleting hook); a reasoning error in the supervisor's own accepted-limitation writeup for that TOCTOU, corrected one round later; a stale doc line plus 2 real, previously-uncovered-but-already-correct code paths; an empty-flag bypass of an argument-exclusivity check; and the plain keyword classifier's own original (round-1) design turning out to share the identical local-text vulnerability class already fixed for its sibling classifier.
- **Supervisor verification discipline:** every fix was independently reproduced with a from-scratch fixture (real concurrent processes, real git hooks at every documented hook type — pre-receive, update, pre-push — real `core.hooksPath` resolution, a real two-process ref-lock race against a bare repo, a 200KB SIGPIPE payload) before being trusted, and every new regression test was mutation-tested by splicing it into the immediately-prior commit's own file to confirm it fails there for the intended reason. `--selfcheck` was re-run 6-15 times per round, never once, because the task's own founding bug (the SIGPIPE) was itself only ~75% reproducible on a single run.
- **Gates:** verify-gate PASS at `7ad2b690d35fbc000898bcff132dc49cc875c8a0` (checked-sha match, 15 real commits) → human hold `83a3148a` answered `merge` (genuine per-task `AskUserQuestion`) → real `MERGE_CONFLICT` in `README.md` against 3 other tasks merged to `main` during this task's own 15-round lifetime (`0.1.26`→`0.1.29`), hand-resolved (adjacent, non-overlapping config-var-table rows, kept both) since `bin/merge-sequencer.sh` itself correctly refuses on any real conflict rather than auto-resolving → merged to main at `f4a56a5`, version bumped `0.1.29`→`0.1.30` → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed → pushed.
- **Escalations:** one genuine per-task merge hold (answered `merge`); one genuine `AskUserQuestion` on the round-14 PATH/supply-chain scope question (answered: accept as documented limitation). No self-answered holds.

## 2026-09-24 — verdict-hardening: file:line-enforced findings validation + --lenses cross-check/ledger for bin/verdict.py

- **Maker (deepseek.v3.2):** implemented `_finding_has_location()`'s file:line/`[NOLOC]` enforcement, `check_lens_coverage()`'s `is True` gate, and the `git-common-dir`-anchored `audit/lens-coverage.jsonl` ledger across rounds 1-5, before ownership transferred to a fork for rounds 6-8 and the merge. Full round-by-round bug list in flow.md.
- **Decisions:** (1) "generalize, don't enumerate," invoked by name from this repo's own `fix-plan-committee-tool-call` precedent — after the URL host:port bypass (round 5→6) and the bare-numeric-pattern bypass (round 6) both hit the identical "what looks like a real path" boundary, the letter-requirement fix was written to close the whole numeric-pattern class at once rather than adding a third literal exclusion; (2) deliberately left one gap open and documented rather than chased further: a domain/email-shaped string with letters and a port (`user@example.com:25`) still passes the shape-only heuristic — framed explicitly in the code comment as "a quality heuristic on top of human supervisor review, not a security boundary," matching the `scope-guard-hook` task's own precedent for naming remaining heuristic gaps; (3) the coordinator's mid-task report of a real cross-task ledger-path collision (17 entries written into a sibling task's primary checkout via a bare relative default) was treated as a confirmed, in-scope defect requiring a fix before merge — not a hypothetical, and not deferred — closed by anchoring via `git rev-parse --git-common-dir`, the exact precedent already established in `claim-ledger.py`.
- **Checker findings resolved, most severe first (rounds 6-8, all real, all independently reproduced by the fork before being fixed):**
  1. URL host:port (`https://bugs.example:443`) accepted as a fake file:line location — fixed by rejecting any matched candidate starting with `//`.
  2. Bare numeric colon-patterns (`10:30`, `16:9`) also accepted — the same boundary question surfacing a third time; generalized to require at least one letter in the matched candidate rather than patching a third specific case.
  3. **The Unicode-vs-ASCII inverse of finding 2:** the letter check used `[A-Za-z]` (ASCII-only), so a real file with a non-ASCII-only name (`λ:12`) was wrongly rejected — found by the checker being explicitly asked to keep probing the exact boundary just generalized. Fixed with `str.isalpha()`. The fork's own first regression-assertion draft used a fixture with a `.py` extension, which still contains ASCII letters and would have silently passed under the OLD broken regex too — caught by mutation-testing the assertion itself (not just the production fix) before trusting it, the fixture then corrected to be ASCII-letter-free.
  4. Checker round 8: clean pass, zero findings, after independently reproducing the Unicode fix and probing further Unicode edge cases (fullwidth digits, Roman numerals, a lone combining mark) with no new bypass found.
- **Gates:** verify-gate PASS at `97782f8` → human hold `497f1957` answered `merge` (genuine `AskUserQuestion`) → first `merge-sequencer.sh` attempt refused on a dirty primary checkout (the new ledger's own untracked default path, correctly anchored but never gitignored — the same class of gap already hit for `.secondmate/`/`audit/merge-ledger.jsonl`/`audit/metrics.jsonl`) → fixed directly on main with a `.gitignore` addition (`5050c72`), no active lock held at the time, confirmed before touching the primary checkout → second attempt hit a real `MERGE_CONFLICT` against the concurrently-merged `lesson-store` task (both branches independently bumped the version/README badge) → resolved by merging `main` into the branch (kept version `0.1.28`, already correctly one past main's `0.1.27`; kept both branches' README selfcheck-chain additions) → re-verified with verify-gate and a live selfcheck run before retrying → merged to main via `bin/merge-sequencer.sh` at `67942da`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed.
- **Escalations:** one genuine per-task merge hold (answered `merge`, real `AskUserQuestion`, not self-answered). No other escalations — the fork made the round 6-8 direct-fix call itself, disclosed prominently rather than silently, given (a) two consecutive rounds of zero progress from a demonstrably pathological maker process and (b) its own standing constraint against pausing mid-task to ask a human for anything short of a genuine blocker.
- **Maker reliability disclosure:** `deepseek.v3.2` failed to commit real, correct work across 4 separate rounds (caught each time via direct `git status`, never trusted from a completion claim), replied DONE without applying a requested one-line fix across one full round, and once degenerated into a genuine infinite-token-repetition model pathology burning real cost with zero output. This is the most severe maker-reliability pattern logged against any single model in this file to date — worth weighing before assigning `deepseek.v3.2` further unsupervised maker rounds without tighter per-round budgets or an earlier abort-and-takeover threshold.

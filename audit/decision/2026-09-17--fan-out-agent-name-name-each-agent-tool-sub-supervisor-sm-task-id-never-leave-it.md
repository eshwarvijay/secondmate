## 2026-09-17 — fan-out-agent-name: name each Agent-tool sub-supervisor sm-<task-id>, never leave it unset

- **Maker (pi/Qwen3-Coder-Next, medium thinking):** appended a three-sentence passage to the end of the Fan-out section's "Mechanism" paragraph in `skills/secondmate/SKILL.md`, instructing the dispatcher to always name each Agent-tool sub-supervisor spawn `sm-<task-id>` and never leave `name` unset. Clean content on round 1; the round-1 checker fail traced to the supervisor's own imprecise task wording, not a maker defect.
- **Decisions:** resolved a genuine ambiguity during plan-committee synthesis (should the outer name differentiate by maker type, matching step (b)'s inner-maker convention) — no: the outer Agent-tool spawn happens before any maker-routing decision exists, so it stays plain `sm-<task-id>` uniformly, explicitly distinguished in the new text from the separate, deeper-nested inner-maker naming; discarded an entire security-theater analysis from one planner (entropy/collision-registry concerns) as inapplicable to a single-session documentation fix.
- **Checker findings resolved:** round 1 correctly flagged that the delivered content was three sentences against the supervisor's own "one sentence" task description — the supervisor recognized this as its own wording imprecision (the consolidated plan, written before the maker was involved, had always specified this exact three-sentence content, each sentence carrying distinct necessary information) rather than routing a content-degrading "fix" back to the maker. Re-ran round 2 with corrected framing asking the checker to judge correctness/completeness rather than literal sentence count; returned a clean pass confirming all three sentences are non-redundant.
- **Gates:** verify-gate PASS at `d2d9d00` → human hold `61d7d274` answered `merge` → merged to main via `bin/merge-sequencer.sh` at `2ec0fe2` → version bump (`0.1.21` → `0.1.22`) → full repo-wide selfcheck chain re-run clean (`ALL_OK`) → `claude plugin validate .` passed → torn down.
- **Escalations:** one genuine per-task merge hold (answered `merge`). No self-answered holds.

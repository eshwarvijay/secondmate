## 2026-09-10 — dispatch-loop: fan-out pattern for 2 concurrent sub-supervisors + bin/dispatch-report.py (3rd of 3 dispatcher primitives)

- **Maker (pi + Claude Sonnet 5, high thinking):** added a new "Fan-out to concurrent sub-supervisors (hard-capped at 2)" section to `skills/secondmate/SKILL.md` and a new standalone `bin/dispatch-report.py` mirroring `bin/verdict.py`'s exact style (stdlib-only, plain `sys.argv`, `selfcheck` with inline asserts) to parse a sub-supervisor's completion tag (`SM_DONE_MERGED:<sha>` / `SM_STUCK_NEED_HUMAN:<reason>` / `SM_REFUSED:<reason>`) into exit codes 0/1/2/3. Required 4 real fix rounds (1 supervisor-found, 3 checker-found across the actual checker's 2 fail rounds) before a clean round-3 pass.
- **Decisions:** (1) full ADHD ideation (5 parallel frames) run before the 6-model plan-committee, per this repo's standing step-0a convention, since "what should this primitive actually be" was genuinely open-ended architecture, not a closed mechanical task; (2) scoped this task to ONLY the spawn-and-track pattern + completion-tag parser, per an explicit human choice via `AskUserQuestion` — a liveness/reaping diagnostic and a dwell-time/staleness sweep (both independently surfaced by the ADHD deepen phase as genuinely valuable) were deliberately deferred as separate future tasks, mirroring `claim-ledger.py`'s own precedent of punting liveness to a future caller rather than solving everything at once; (3) hard-capped concurrent sub-supervisors at N=2 (not a tunable parameter), per an explicit human choice, directly responding to the ADHD provocation that every deepened branch converged on the same real ceiling — human hold-answering attention, not concurrency-safety, is what actually limits safe fan-out, and this repo had already self-caught itself rubber-stamping a hold once under single-task load; (4) rejected two planner-suggested alternatives during plan synthesis: repurposing `bin/verdict.py` itself to also parse completion tags (blurs its existing single-purpose contract) and a filesystem-event-file coordination layer (adds a new race and a new state dependency neither ledger needs); (5) discarded a large amount of planner-generated security theater (SQL injection, IDOR/ownership tables, a `claim_ownership` table) as inapplicable — this is a flat-file, single-operator, local-git system with no multi-user auth model, and claim-ledger.py's own existing task-id format validation already closes the one real injection-adjacent concern (downstream names are only ever derived from an already-validated task-id).
- **Checker findings resolved, most severe first (all real except one adjudicated-not-a-bug and one adjudicated-not-actionable, all independently reproduced by the supervisor before being routed to the maker):**
  1. **(supervisor-found, pre-checker):** missing `.claude-plugin/plugin.json` version bump / README badge sync on the maker's first commit, versus this repo's own standing per-merge convention. Fixed before the checker ever ran.
  2. **(checker round 1, most severe):** the new SKILL.md fan-out section ordered `bin/merge-sequencer.sh` (step d) before opening the human `bin/hold.py` entry (step e) — read literally, a sub-supervisor could merge before a human ever approved, inverting the existing single-task loop's own Gate → Hold → Integrate contract. Confirmed via direct diff read before routing the fix; reordered so the hold is opened and answered before `merge-sequencer.sh` is ever called.
  3. **(checker round 1, adjudicated NOT a bug):** `dispatch-report.py` accepting `SM_DONE_MERGED:not-a-sha` with exit 0 — the payload is deliberately opaque/unvalidated by design, mirroring `bin/verdict.py`'s own established philosophy. Left unchanged.
  4. **(checker round 2):** the identical merge-before-hold ordering bug, independently duplicated in `docs/ARCHITECTURE.md`'s own prose summarizing the same steps — never updated when SKILL.md was fixed in the prior round. Confirmed by direct read, fixed the same way.
  5. **(checker round 2, same round):** SKILL.md's corrected `bin/hold.py hold` example command was missing hold.py's two REQUIRED arguments (`--task`, `--q`) — confirmed by the supervisor directly running `hold.py hold --help`, and noting the SAME file already showed the correct full form elsewhere that this new section's example simply didn't match.
  6. **(checker round 2, adjudicated NOT actionable):** no automated regression test for the SKILL.md prose-ordering fix — pure markdown documentation has no executable code path to assert against; checker review is the appropriate control, matching the `harden-checker-invariant` task's identical precedent.
  7. **Checker round 3: clean pass, zero findings**, after being explicitly asked to cross-check every other example command in both changed files against each referenced script's real `--help` output — none found.
- **Gates:** verify-gate PASS at `9c61307` (checked-sha match) → human hold `857f8188` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` (its second real production use) at `31c519a`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit, independently by the supervisor → `claude plugin validate .` passed → torn down.
- **Escalations:** two genuine `AskUserQuestion`s before build started (task scope: build only the spawn-and-track pattern now, defer the other two ADHD-identified pieces; safe fan-out N=2), plus one genuine per-task merge hold. No self-answered holds.
- **Side incident (infrastructure, not this task's deliverable):** this session's batch-scoped `caffeinate-guard.sh` process had died by teardown time — its 8-hour TTL elapsed during an unusually long session (ADHD ideation + 6-model committee + 3 checker rounds on top of everything already done earlier). Restarted immediately since teardown was still in progress; the script behaved exactly as designed (a bounded ceiling, not a permanent guarantee), not a defect.

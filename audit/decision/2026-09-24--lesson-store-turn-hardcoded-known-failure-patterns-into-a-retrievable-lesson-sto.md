## 2026-09-24 — lesson-store: turn hardcoded "Known failure patterns" into a retrievable lesson store

- **Maker (pi/Qwen3-Coder-Next, medium thinking):** built `bin/lesson-lookup.py` (Python, not bash — frontmatter parsing/tokenization in bash was independently flagged as fragile by two of six planners) and `bin/lessons/<category>/<slug>.md`, migrating the 4 hardcoded SKILL.md bullets verbatim as `evidence: E4`/`earned-in: seed` lessons, then wired all 7 SKILL.md occurrences to a real shell command substitution calling the new script. Run entirely by a fork given full supervisor ownership of this task, freeing the coordinator for concurrent work.
- **Decisions:** (1) the plan committee's most important finding — a naive "top-3" cap would silently drop one of only 4 proven lessons on day one — resolved by making `evidence: E4` mean "always included, never capped," with only non-E4 lessons subject to relevance scoring; this gives the evidence-tier field real semantic weight rather than treating it as decorative metadata, and produces exactly zero regression today; (2) a total-failure fallback (missing directory, permission error, any internal exception) hardcodes the original 4 lessons' literal text directly in the script, independent of any file read, so a completely broken lesson store degrades to exactly today's static behavior rather than an empty prompt block; (3) declined bash-only implementation per two independent planner reviews flagging bash frontmatter/tokenization as fragile — used Python matching this repo's own convention for text-heavy `bin/` tools (`hold.py`, `verdict.py`, `claim-ledger.py`, `committee-output.py`); (4) explicitly deferred automatic lesson creation from checker fail-verdicts as a separate future task, matching the original steal-list's own scoping.
- **Checker findings resolved, most severe first (all real, all independently reproduced by the fork before being routed to the maker or accepted as a checker claim):**
  1. **(fork-found, pre-checker):** all 7 newly-wired SKILL.md substitutions were missing the inner `$` before `{CLAUDE_PLUGIN_ROOT}` — confirmed via direct execution to cause an outright bash parse error, not merely empty output. Fixed in all 7 places.
  2. **(checker round 1):** `parse_frontmatter()` silently discarded tags for every real lesson file (multi-line YAML list form never handled) — confirmed on the real shipped `commit-before-done.md` file both before (`tags: []`) and after (`tags: ['maker','workflow','commitment','done']`) the fix, plus a from-scratch fixture proving tag-scoring genuinely overrides filename tie-break.
  3. **(fork-found, between rounds):** the maker's own new regression test for finding 2 passed vacuously — searching printed output for tag *values* that `build_output()` never actually prints, so a `-1` "not found" sentinel always satisfied the `<` comparison regardless of whether tag scoring worked. Fixed with unique body markers and explicit presence assertions.
  4. **(checker round 2, most severe):** the corrected test from finding 3 was itself never called in `selfcheck()` — genuinely dead code; the fork's own earlier "mutation-test confirmation" had only ever exercised a different, coincidental assertion. Fixed by adding the missing call; re-mutation-tested specifically, confirmed the failure now genuinely originates from that test's own assertion line.
  5. **(checker round 2, same round):** `README.md`'s manual verify-everything chain omitted `bin/lesson-lookup.py selfcheck`, violating this repo's own doc-sync convention. Fixed.
  6. **Checker round 3: clean pass, zero findings**, after independently confirming via AST inspection that every defined test helper is genuinely invoked and independently re-mutation-testing the tag-scoring path.
- **Merge-reconciliation:** a real `MERGE_CONFLICT` on the first `merge-sequencer.sh` attempt (main had advanced via a concurrently-merged sibling task's own version bump) — resolved by merging `main` into the branch directly, two trivial version-string conflicts, `docs/ARCHITECTURE.md` auto-merged cleanly, both this task's and the sibling's selfchecks re-run clean against the merge commit before retrying the merge successfully.
- **Gates:** verify-gate PASS at `67f5de4` and again at `8fe7cf9` (post-reconciliation) → human hold `1997717b` answered `merge` (genuine `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `5c35129`, pushed → full repo-wide selfcheck chain re-run clean on the actual merged commit → `claude plugin validate .` passed → torn down.
- **Escalations:** one genuine per-task merge hold (answered `merge`). No self-answered holds.

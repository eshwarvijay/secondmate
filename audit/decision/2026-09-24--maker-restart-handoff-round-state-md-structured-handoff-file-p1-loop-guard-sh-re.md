## 2026-09-24 — maker-restart-handoff: round-state.md structured handoff file (P1) + loop-guard.sh RESTART/exit-5 signal (P2)

- **Maker (pi/Qwen3-Coder-Next, high thinking):** wired `round-state.md` read-injection + write-instruction at all 7 SKILL.md maker-launch prompt sites, and restructured `loop-guard.sh action`'s exit-code case statement so every `n` in `3<=n<ABORT_REPEATS` emits a machine-parseable `RESTART`/exit-5 signal (closing the old silent gaps at n=4,6,7,9), rather than the exit-3 hard-abort being the only non-silent outcome.
- **Decisions:** (1) treated every maker commit message in this task as an unverified claim requiring independent diff/execution confirmation, not a status report — this repo's own established discipline, but exercised unusually heavily here since 3 separate commits in this one task described work their own diffs did not contain; (2) when the maker's second fix attempt spiraled into ad-hoc scratch-script debugging past 80% context, the fork traced the actual root cause itself (an unguarded `set -e`-fatal call plus a stateful-recall double-increment bug) and wrote+mutation-tested a complete replacement selfcheck block BEFORE handing anything back — matching this repo's "give exact verbatim code after a stuck-loop recurrence" precedent, escalated one level further to a fully mechanical write-script-and-run instruction (not prose-and-paste) after that verbatim-text handoff ALSO produced a mismatched commit; (3) accepted one checker-flagged SPEC AMBIGUITY without a fix — `ABORT_REPEATS<=2` is an unrealistic config value where the `n<3` silent-exit clause pre-empts what would otherwise be an immediate hard-abort, judged not worth chasing given the only tested/documented value is the default 10, matching this repo's precedent for narrow impractical edge cases (`pi-scope-guard`'s `/scope-guard-marker` doc mismatch, `sync-worktree-skills`'s ancestor-dangling-symlink gap).
- **Checker findings resolved, most severe first (round 1, real cross-model `gpt`, both independently reproduced by the fork before being routed to the maker):**
  1. All 5 pi-maker prompt-assembly sites (2 initial-route + 3 fix-round loop-back) were missing the round-state WRITE instruction entirely — only the 2 Claude-maker sites had it, meaning the entire P1 mechanism silently depended on the supervisor manually remembering to add it by hand for every pi-routed task, the exact dependency this task exists to remove. Confirmed via direct `sed`/grep inspection of the actual prompt text at each site before routing.
  2. The new `loop-guard.sh` selfcheck only exercised the exit-5/exit-3 boundary at one `ABORT_REPEATS=4` value — a real regression-coverage gap versus the fork's own explicit HAMMER instruction, though the underlying exit-code logic itself was already independently confirmed correct via a direct 12-point mutation walk before the checker ever ran.
- **Post-round-1 fix-verification findings (all fork-found via direct diff/execution, none from a checker round, all real):**
  3. The maker's first fix-#2 commit message claimed the selfcheck was expanded; the actual diff touched only SKILL.md, `bin/loop-guard.sh` byte-identical to the prior commit — plus the fix-#1 commit had also left 1 of 7 write-instruction sites (a fix-round loop-back "Claude maker" duplicate) unfixed. Both caught by direct `git show --stat`/grep before any further routing.
  4. The maker's second attempt at the selfcheck expansion entered a genuine debugging spiral (multiple scratch `track-*.sh` files, context past 80%) before the fork independently traced and fixed the real root cause itself (an unguarded `set -e`-fatal boundary call; a stateful "recheck the message" call silently double-incrementing the counter) and handed over a complete, pre-mutation-tested verbatim replacement — whose resulting commit message correctly described this exact fix, but whose diff, a THIRD time in this task, touched only SKILL.md (a real, separate, correctly-described fix to the last missing write-instruction site) while leaving `bin/loop-guard.sh` untouched yet again.
  5. Resolved by restarting the maker fresh on the same pane with a fully mechanical, zero-judgment instruction (write a self-contained Python splice script, run it, verify `ok`, commit) rather than another prose instruction into an already-degraded context — this succeeded on the first attempt, independently confirmed byte-identical to the fork's own pre-verified template.
  6. Checker round 2: clean pass, zero findings, after independently re-walking the full boundary at 4 different `ABORT_REPEATS` values and confirming 7/7 read+write site pairing by exact line number.
- **Gates:** verify-gate PASS at `798ce34` (pre-reconciliation) and `bedbc73` (post-reconciliation) → human hold `caa994ae` answered `merge` (genuine `AskUserQuestion`, not self-answered) → real `MERGE_CONFLICT` against the concurrently-merged `verdict-hardening` task's version bump, resolved by merging `main` into the branch (version → `0.1.29`, one past main's `0.1.28`) → merged to main via `bin/merge-sequencer.sh` at `7aefd2d`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed.
- **Escalations:** one genuine per-task merge hold (answered `merge`, real `AskUserQuestion`). No self-answered holds — the fork's own root-cause tracing and mechanical-restart decisions were within its standing autonomous-execution authority for this task, each disclosed prominently in the audit trail rather than silently absorbed.
- **Maker reliability disclosure:** across this one task, the pi maker's own commit messages described work its diff did not contain on 3 separate occasions — a distinct failure mode from `deepseek.v3.2`'s non-commitment/pathological-repetition failures logged in `verdict-hardening` immediately above, but comparably severe: a commit message is the ONLY signal a supervisor has without re-diffing every round, and this task's fork treated that signal as unverified every single time, which is what caught all 3 instances before any of them reached a checker or a merge.

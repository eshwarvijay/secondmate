## 2026-09-24 — supervisor-mistake-lessons: moment-of-action echoes (P1) + lesson feedback loop (P2)

- **Maker (Claude Sonnet 5):** implemented P1 (moment-of-action echoes: `hold.py answer`'s genuine-human-decision reminder, `merge-sequencer.sh`'s pre-merge version/docs-sync reminder — never on `--preflight-only` — and a new `bin/teardown-check.sh` advisory scan wired into SKILL.md step 9) and P2 (a lesson feedback loop: `lesson-lookup.py --task-id`-keyed injection logging anchored via `git rev-parse --git-common-dir` matching `claim-ledger.py`'s own precedent, a new `tag --lesson-id --outcome helpful|harmful` subcommand atomically incrementing per-lesson frontmatter counters via a surgical text edit, a never-helpful-bucket sort deprioritization that never excludes an E4 lesson, `log-round.sh`'s new repeatable `--lesson-id` flag, and a new SKILL.md "Lesson feedback" step with an explicit ACE-style evidence-discipline requirement). Required 8 real correction rounds across 9 checker rounds — see `audit/flow/2026-09-24--supervisor-mistake-lessons.md` for the full round-by-round technical detail.
- **Decisions:**
  1. Tagging a lesson helpful/harmful is explicit SUPERVISOR JUDGMENT, made by direct observation during that specific task — never an automated correlation against `verdict.py`/`audit/metrics.jsonl` history. This was locked into the original task brief and re-affirmed explicitly to the maker every round; no correlation-detection logic exists anywhere in the shipped diff, matching the ACE/agentic-context-engine `tag_skill` pattern this mechanism is modeled on (leaving a lesson untagged is always fine, tagging on a guess is not).
  2. The `merge-sequencer.sh` pre-merge reminder is a STATIC textual echo only — it never inspects/diffs `plugin.json` or README against git history to verify they were actually updated; building that would be a materially larger feature than "moment-of-action echo" ever intended, and none of these new checks can verify who is at the keyboard or whether a human genuinely acted on the reminder — all are advisory, none block a caller that would otherwise succeed.
  3. **Round 1's own checker addendum wording was corrected mid-task, not the maker's implementation:** the addendum said "all new checks must never block a caller," which caused the checker to flag `teardown-check.sh`'s intentional nonzero-exit-on-leftover-state as a defect. The sub-supervisor recognized this as its own imprecise framing (the original task brief always specified "advisory-nonzero-exit, not a hard lock — the supervisor decides what to do with the signal") and corrected the checker's framing in round 2 rather than degrading the maker's already-correct implementation — directly matching this repo's own `fan-out-agent-name` precedent for exactly this class of judgment call.
  4. **Round 7's checker-pane coverage gap was adjudicated an accepted, documented limitation, not a bug to route back to the maker.** `_check_herdr` covers maker agents only, never a checker pane, because no task-id-derived pane identity exists anywhere in this repo's herdr integration; closing it for real would mean inventing a brand-new pane-naming/discovery convention touching `herdr-pane.sh`'s split call AND the shared SKILL.md "Checker pane" recipe every task in this repo uses — the sub-supervisor independently checked `herdr pane rename`/`herdr pane list` before making this call (confirming it was a genuine scope decision, not a lazy dismissal) and instructed the maker to document, not build. Round 8's checker independently confirmed the resulting documentation (in the script's own header, README, and ARCHITECTURE) was adequate and prominent, and did not re-raise the finding.
- **Checker findings resolved, most severe first (all real, all independently reproduced by the sub-supervisor with a from-scratch fixture BEFORE routing to the maker, and again AFTER the fix, never trusted from the checker's claim or the maker's report alone):**
  1. A concurrent lost-update race in `cmd_tag`'s read-increment-write cycle (20 real concurrent processes landed at `helpful_count=8`, not 20) — closed with this file's own existing `_ledger_lock()` fcntl idiom, locked per lesson-file path.
  2. A missing per-segment length bound on `_LESSON_ID_RE` (a 129-char segment accepted) — closed matching `claim-ledger.py`'s own 1-128-char precedent.
  3. An unhandled `OSError: File name too long` crash from many individually-valid segments joining into an overlong overall path — closed with a layered defense: a 255-char total-length cap checked first, plus an outer `OSError` backstop as defense in depth (discovered the same underlying OS failure could surface from a second call site, `_ledger_lock`, not just the length-capped resolver).
  4. `_check_herdr` reporting a false `clean` for a genuine herdr-daemon-call failure — closed by requiring the CONFIRMED `agent_not_found` JSON error shape (verified directly against a real herdr 0.8.0 install) before ever reporting "absent"; anything else reports `unknown`.
  5. A SIGPIPE-under-`pipefail` race in `_check_worktree`/`_check_claim`'s `producer | grep -qF` pipes, misreporting a genuinely present worktree/claim as clean — closed by eliminating the pipe entirely (capture-then-match), the same technique this repo's own `merge-sequencer.sh` (task `git-coordinator`) already used for the identical bug class the same day.
  6. That same fix's own `|| true` silently swallowing a genuine producer failure (not just SIGPIPE) as a false `clean` — generalized to a real 3-way present/clean/unknown contract matching `_herdr_agent_state`'s own convention, for both affected checks.
  7. A substring-match false positive in `_check_worktree` (task-id `foo` matching an unrelated `sm/foo-bar` worktree) — closed with a genuinely exact per-line match; `_check_branch`/`_check_claim` independently re-verified immune to the same collision class by direct reproduction, not just by re-reading the code.
  8. A fallback-path injection-logging gap in `lesson-lookup.py` (both the missing-store and exception-catch-all paths skipped `--task-id` logging entirely) — closed by defining the 4 canonical fallback lesson-ids and best-effort-logging them on both paths via a shared `_log_injection_safe()` helper.
  9. Round 9: clean pass, zero findings, after an explicit full fresh pass across the entire accumulated diff with an explicit ship recommendation.
- **Gates:** verify-gate PASS at `aacdc5e0c77414597bdc172a61b0973710740464` (checked-sha match) → human hold `8f05050f` answered `merge` (genuine hold-wait-resume cycle — polled with no answer for ~5 minutes, reported `SM_STUCK_NEED_HUMAN:8f05050f` and ended the turn rather than guessing; the resuming `merge` answer was independently re-verified against the raw `decisions.jsonl` ledger, both the `hold`/`answer` event pair and `bin/hold.py open`'s empty result, before acting — never trusted from the coordinator's relay message alone) → merged to main via `bin/merge-sequencer.sh`.
- **Escalations:** one genuine per-task merge hold (answered `merge`, independently re-verified from the raw ledger). No `AskUserQuestion`-style escalations were available or used in this sub-supervisor context; both judgment calls above (the checker-addendum correction, the checker-pane accepted-limitation adjudication) were made directly by the sub-supervisor and disclosed in full rather than escalated or silently absorbed, matching the established precedent from this same day's `fix-scope-guard-marker-doc`/`fix-doctor-status-line` sub-supervisor tasks for exactly this class of in-context judgment call.
- **Lesson feedback (step 11):** the maker was prompted with the 4 seed lessons (`commit-before-done`, `mutation-test-your-tests`, `stay-in-literal-scope`, `avoid-ad-hoc-debug-loops`, logged to the injection ledger under this task-id at every round) — deliberately, no lesson was tagged helpful or harmful this task. Sub-supervisor's own reasoning, applying the evidence-discipline requirement this same task just shipped: none of the 4 described mistakes recurred (the maker committed cleanly every round, its diffs stayed in scope, no fake test was ever caught, no debugging-loop stall occurred) — but ABSENCE of a mistake is weak evidence at best that a specific lesson CAUSED the good outcome, as opposed to the maker simply not needing the reminder that round. Tagging "helpful" on that basis would be exactly the ungrounded, hunch-based tag the evidence-discipline requirement exists to forbid. This is the expected, disciplined common case, not a gap — most tasks should leave most lessons untagged; only a clear-cut recurrence-despite-injection (harmful) or a specific, attributable avoidance the maker itself flagged (helpful) should ever be tagged.

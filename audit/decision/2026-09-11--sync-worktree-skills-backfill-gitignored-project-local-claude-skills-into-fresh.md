## 2026-09-11 — sync-worktree-skills: backfill gitignored project-local .claude/skills/ into fresh worktrees

- **Maker (pi + Claude Sonnet 5, high thinking):** built `bin/sync-worktree-skills.sh` and wired it into `bin/new-worktree.sh` and `skills/secondmate/SKILL.md`'s herdr Spawn step, fixing a live-reported real bug (`Unknown skill: agent-analytics/.claude/skills/emil-design-eng`, hit by the human while using secondmate's worktree pattern in an unrelated repo, `archie`). Root-caused by the supervisor before scoping any task: `.claude/skills/` is a common gitignore convention, and `git worktree add` (used by both this repo's own headless fallback and the external `herdr worktree create` tool) never propagates untracked content into a new worktree. Required 4 real fix rounds across 3 real checker rounds.
- **Decisions:** (1) copy, never symlink — an explicit human choice after the supervisor flagged that a symlink pointing back outside the worktree would be resolved and denied by `bin/scope-guard.py`'s own existing path-confinement check; (2) scope narrowly to `.claude/skills/` only — an explicit human choice after the supervisor laid out the real repo's own `.gitignore` and showed the other candidate paths (settings.json/settings.local.json, session-state files) were either sensitive or actively wrong to duplicate into a fresh worktree; (3) the initial hypothesis for the reported symptom (a Claude Code CLI "SKILL ACTIVATION CHECK" suggestion-hook mismatch) was investigated, confirmed as a genuinely real but SEPARATE and out-of-scope issue (no file anywhere in the accessible filesystem backs the mismatched suggestion names — a built-in CLI behavior, not a secondmate-controllable bug), and explicitly set aside once the human supplied the concrete error message that led to the real root cause.
- **Checker findings resolved, most severe first (all real, all independently reproduced by the supervisor against BOTH a synthetic fixture AND the actual real-world `archie` repo/worktree before being routed to the maker):**
  1. **(supervisor-found, before any checker round):** round 1's implementation passed its own synthetic selfcheck cleanly but did NOTHING against the real `archie` case — the real skill entry is itself a symlink (`.claude/skills/emil-design-eng -> ../../.agents/skills/emil-design-eng`), and round 1's enumeration filtered strictly on `-type d`, silently excluding symlinks. Fixed by resolving symlinked entries via `python3 os.path.realpath` and materializing the real resolved content as a genuine directory (never a symlink, since the resolved target was itself also gitignored and also missing from the worktree).
  2. **(checker round 1, most severe):** a symlink resolving OUTSIDE the primary checkout entirely was silently followed, materializing arbitrary external filesystem content into every new worktree — a real security-relevant leak, reproduced directly by the supervisor with a fixture symlink to sensitive external content before routing. Fixed with a boundary check against the canonicalized `--primary` root.
  3. **(checker round 1, same round):** `bin/new-worktree.sh --selfcheck`'s existing test suite had zero coverage for the new wiring call — confirmed by the supervisor via the identical mutation (neutralizing the call to a no-op still passed) before routing. Fixed with a real fixture-based assertion exercising the actual code path.
  4. **(checker round 2):** the existence guard `[ -e "$target" ]` follows symlinks, so a dangling symlink already at a target path wasn't recognized as "present" — the script then tried to `mv` real content onto it and failed, which (since `new-worktree.sh` runs under `set -e`) would abort the entire worktree-creation call, not just skip one sync. Reproduced by the supervisor with the exact scenario before routing. Fixed by widening the guard to `[ -e ] || [ -L ]`.
  5. **Checker round 3: clean pass**, after independently re-running every prior mutation test and auditing for sibling instances of the same `-e`-without`-L` bug class (found none). One spec-ambiguity noted but not treated as a defect: a tracked dangling symlink at an ancestor directory (not the target) blocking parent creation — the documented contract covers existing targets, not existing ancestors; left as a named, accepted gap.
- **Gates:** verify-gate PASS at `9f6504b` (checked-sha match) → human hold `b454cb3c` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` (its fourth real production use) at `e7cd88a` → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual final state, independently by the supervisor → `claude plugin validate .` passed → torn down.
- **Process note (self-disclosed, repeat of a prior-task gap):** the supervisor again forgot to bump `.claude-plugin/plugin.json`'s version on the initial merge, the identical class of gap already caught once this same session (`dispatch-loop`). Caught and fixed directly (`0.1.18` → `0.1.19`) along with the README entries the merge had also missed, in a small separate follow-up commit (`cde10d1`) — not routed through a fresh maker/checker cycle, matching this repo's own established precedent that pure version/doc metadata bumps are a direct supervisor action, not project code.
- **Escalations:** two genuine `AskUserQuestion`s before implementation started (copy-vs-symlink approach; scope of what else to sync), plus one genuine per-task merge hold. No self-answered holds.

## 2026-09-24 — audit-trail-restructure: restructure secondmate's audit trail into a lookup-only, generic mechanism (bin/audit-log.py)

- **Maker (Claude, `--permission-mode auto`):** built `bin/audit-log.py` (stdlib-only Python, `add`/`show`/`list`/`search`/`reindex`/`migrate`/positional `selfcheck` subcommand — the last matching this repo's own dominant Python-CLI convention, `bin/claim-ledger.py`/`bin/hold.py`/`bin/dispatch-report.py`, not the bash-script `--selfcheck` flag form), migrated both real monoliths verbatim into per-task files, generated a bounded `audit/INDEX.md`, and rewired `CLAUDE.md`/`skills/secondmate/SKILL.md` step 10/`README.md`/`docs/ARCHITECTURE.md`. Required 5 real fix rounds across 6 checker rounds.
- **Supervisor scope/design adjudications (not bugs, none escalated — each matched an established repo precedent for a checker finding that reflected the ask's own imprecision or a legitimate scope boundary, not a real defect):**
  1. The checker's finding that `bin/audit-log.py --selfcheck` (double-dash) isn't accepted, only the positional `selfcheck` subcommand, was adjudicated NOT a bug on round 1 — the positional form matches this repo's own dominant Python-CLI convention (`claim-ledger.py`, `hold.py`, `dispatch-report.py` all use it); only bash scripts and one outlier Python script (`committee-output.py`) use `--selfcheck`. The maker was told explicitly not to add the flag form.
  2. The checker's round-2 finding that `cmd_add` strips outer body whitespace via `.strip()` was adjudicated NOT a bug — the task's explicit "never delete real historical detail, move verbatim" constraint was specifically about the one-time `migrate` command preserving OLD historical entries byte-for-byte; it was never a promise about NEW `add`-created bodies' exact outer whitespace, and `add`'s own docstring makes no verbatim promise. This finding recurred (re-raised) in rounds 3 and 4's "CLEAN" sections without ever being re-flagged as a defect once the checker was told the adjudication stood.
- **Checker findings resolved, chronologically (all real, all independently reproduced by the supervisor via a from-scratch repro AND mutation-tested — reverting each fix and confirming the corresponding new selfcheck assertion then genuinely failed with the predicted symptom — before being trusted or routed):** see `audit/flow/2026-09-24--audit-trail-restructure-...md` for the full round-by-round technical detail (5 in round 1, 3 in round 2, 2 in round 3, 2 in round 4, 1 in round 5, 0 in round 6). Two structurally notable findings: (a) round 4's task-id filename collision (`a_b` vs `a-b`, via the lossy `_slugify()` helper) and round 5's sibling case-collision (`Task-A` vs `task-a`) are the same underlying failure class recurring twice, one dimension apart — the round-5 fix closed it at the true root cause (the underlying filesystem is case-insensitive, confirmed directly by the supervisor, so no amount of in-process case handling could have actually prevented the collision; the fix had to reject uppercase task-ids at the validation gate instead); (b) round 4's `assert code == 0 and "no matches" not in out.lower() or True` had been a genuinely vacuous, zero-coverage assertion since round 0 due to Python's `and`/`or` precedence — undetected by 3 prior checker rounds until round 4 specifically hunted for tautological assertions after being asked to.
- **Gates:** verify-gate PASS at `d6c01f9512a509f0c718fa78e5da276767165cd6` (checked-sha match) → human hold `a3d6bec9` (bound to that sha via `--sha`) → the sub-supervisor polled `bin/hold.py open` every 30-120s for ~5 minutes with no answer, released its claim, and reported `SM_STUCK_NEED_HUMAN:a3d6bec9` rather than assume or guess an answer → later resumed via a coordinator message claiming the hold was answered `merge` — the sub-supervisor independently re-verified this against the raw `decisions.jsonl` ledger record (`{"ev":"answer","id":"a3d6bec9",...,"a":"merge"}`) and `bin/hold.py open` returning empty, before re-claiming the task-id and proceeding → merged to main via `bin/merge-sequencer.sh` at `98c4f0389a7b45e3b9ed20641dc9ff1446893449`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit → `claude plugin validate .` passed.
- **Escalations:** one genuine hold-wait-resume cycle (the hold was NOT self-answered; the sub-supervisor genuinely stopped and waited, matching this repo's own established `fix-doctor-status-line`/`fix-scope-guard-marker-doc` precedent for a real `SM_STUCK_NEED_HUMAN` cycle under the fan-out pattern). No self-answered holds.
- **Meta-note:** this is the first `decision.md`-style entry written using the very mechanism (`bin/audit-log.py add`) that this task itself built, replacing the append-to-monolith convention these entries used for every prior task in this repo's history.

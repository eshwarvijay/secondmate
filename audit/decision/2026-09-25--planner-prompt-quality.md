## 2026-09-25 — planner-prompt-quality: evidence-cited upgrade to the 6 plan-committee system prompts

- **Maker (Claude):** upgraded the 6 per-model planner prompts in `bin/plan-committee.sh` using research-sourced patterns rather than generic polish — negation cleanup, a Contrastive-CoT INVALID/VALID worked example (mistral-large3 and qwen3-80b only), a domain-applicability gate (same two), and an `ASSUMED:` prefix convention (all 6). 3 rounds.
- **Decisions:** (1) skip the formal plan-committee ceremony — the research pass already produced a complete, concrete spec, matching this session's own precedent for skipping it when investigation has resolved the design questions; (2) adjudicated one round-2 checker finding as NOT a bug — the checker flagged `ASSUMED:` appearing on qwen3-coder/kimi-k3 as a spec violation, reading only one half of a self-contradictory sentence in the supervisor's own original task instruction; the same instruction's parenthetical explicitly said the `ASSUMED:` convention applies to all 6, so the maker's implementation matched the supervisor's actual intent — the wording flaw was the supervisor's, not a defect in the diff, so no fix was routed for it.
- **Checker findings resolved, most severe first (all real except the one adjudicated above, all independently reproduced by the supervisor before or after routing):**
  1. **(round 1):** the qwen3-80b Contrastive-CoT INVALID example was fabricated — described a `jsonschema`/`SchemaValidator`/`bin/schema-utils.py` scenario that never happened, instead of the actual documented incident (a shared `bin/utils/` function citing nonexistent `verify-worktree.ts`/`clean.ts`, from `audit/flow/2026-09-11--fix-scope-guard-marker-doc...md`) that the original task instruction explicitly asked the maker to use. Confirmed by the supervisor via direct `grep` against the real audit record before routing.
  2. **(round 1, same round):** the mistral-large3 VALID example self-contradicted its own domain-applicability gate — claimed "file paths derived from a CLI arg" for a task text that only said the flag "prints planned file writes," inventing an unstated mechanism instead of grounding the finding in the literal task text as the gate itself required. Confirmed by the supervisor via direct re-read of the example against its own stated task text.
  3. **(round 2):** the new selfcheck assertions for both fixed examples only checked generic structural markers, not the specific documented-incident content — a future edit could silently regress either example back to its fabricated form while `--selfcheck` still reported `ok`. Fixed with content-anchored, mutation-tested assertions (real-incident details present, fabricated details absent) for both.
  4. **Round 3: clean pass, zero findings.**
- **Gates:** verify-gate PASS at `e58ccd64bb9ec1edc7ad69463b4fbeb91294fbe0` (checked-sha match) → human hold `13caa011` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` at `ad8eec3`, pushed → worktree/branch/pane torn down.
- **Escalations:** one genuine per-task merge hold (answered `merge`). No self-answered holds. One clarifying `AskUserQuestion` before implementation started (confirming the user wanted the per-model system prompts themselves upgraded, not the `--task` description text the supervisor writes per invocation, after the supervisor's own read showed the existing prompts were not actually vague).

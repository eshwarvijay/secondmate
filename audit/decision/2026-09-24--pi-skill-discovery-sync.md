## 2026-09-24 — pi-skill-discovery-sync: close pi-vs-Claude-Code skill discovery gap + harden doctor.sh heal-fix against symlink escapes/TOCTOU (fan-out mid-task handoff)

- **Maker (Claude, Sonnet 5):** implemented the pi-visibility sync extension to `bin/sync-worktree-skills.sh` (`.claude/skills/`, `.pi/skills/`, `.agents/skills/`, any depth) and the new `bin/doctor.sh` skill-discovery diagnostic (`_detect_skill_discovery_sync`/`_skill_discovery_heal_fix`) across 6 rounds, plus a `.pi/skills/` fixture in `bin/new-worktree.sh`'s own selfcheck and doc/version sync. Required 5 real correction rounds before a clean round-6 pass — see `audit/flow/2026-09-24--pi-skill-discovery-sync.md` for the full round-by-round technical detail.
- **Checker findings resolved, most severe first (all real, all independently reproduced by the sub-supervisor with a from-scratch fixture BEFORE routing to the maker, and again AFTER the fix, never trusted from the checker's claim or the maker's report alone):**
  1. `_detect_skill_discovery_sync`'s top-level-only scan silently missed nested monorepo skills already synced by `bin/sync-worktree-skills.sh` — closed by generalizing to the identical any-depth scan, keyed by `(relative-parent-prefix, name)` to avoid collisions between same-named skills in different subprojects.
  2. **The task's most severe finding:** the heal-fix command only containment-checked its SOURCE, never its DESTINATION — a symlinked `<prefix>/.agents` pointing outside the repo let real skill content leak outside the checkout via `--heal --yes`. Closed by walking to the destination's nearest existing ancestor and containment-checking it the same way the source already is.
  3. A TOCTOU in finding 2's own fix: the containment check only held at fix-string-generation time, but the fix string is documented as inspectable/deferrable (readable via `--json`, runnable by a human at an arbitrary later time) — a filesystem change between generation and execution could still leak content through an already-generated, now-stale string. Closed structurally (not by narrowing the window): the same containment logic is re-run INSIDE the generated command, at actual execution time, via an embedded `python3 -c` script that recomputes everything fresh rather than trusting the earlier decision.
  4. A confirmed test-coverage gap (the execution-time re-check already correctly refused a stale SOURCE too, but no test exercised it) closed with a symmetric regression test, no production change.
  5. A second confirmed test-coverage gap (the embedded python's generic exception handler around `mkdir`/`cp` was reachable and already fail-closed, e.g. if `cp` is missing, but untested) closed the same way — pure test addition.
  6. Round 6: clean pass, zero findings, after being explicitly asked to review the full accumulated 6-commit diff (not just the latest round) and confirm `bin/sync-worktree-skills.sh`/`bin/new-worktree.sh` stayed byte-identical to the round-1 commit.
- **Sub-supervisor scope/judgment decisions (not escalated via `AskUserQuestion` — no such channel exists in this sub-supervisor context; `bin/hold.py` is reserved for the merge decision per the fan-out pattern's own contract, matching the established precedent from `fix-scope-guard-marker-doc`/`fix-doctor-status-line` for making this class of call without escalation):**
  1. **Accepted, documented (not chased further) residual limitation:** round 4's checker raised a SUSPECTED (not deterministically reproduced) intra-process race — the embedded python's own realpath-check-then-`mkdir`/`cp` is still two separate syscalls within one process, so a theoretical adversary with concurrent local filesystem write access at that exact instant could still win a race. Given (a) three full rounds had already gone into hardening this exact function, (b) the precondition (adversary already has concurrent local write access to the checkout) matches the same class of risk this codebase has repeatedly accepted-and-documented rather than eliminated elsewhere (`bin/scope-guard.py`'s symlink TOCTOU, `bin/caffeinate-guard.sh`'s PATH/supply-chain trust assumption, this same session's `git-coordinator` round-14 precedent, human-ratified there), the sub-supervisor instructed the maker to document this by name in the function's own comment rather than attempt a more invasive atomic-open/fstat rewrite. The following round's checker explicitly accepted this framing (an `N/A / accepted residual limitation` line in its coverage table) rather than re-raising it.
  2. **Out-of-scope determination, verified not assumed:** a checker-found, independently-reproduced-real bug in `bin/sync-worktree-skills.sh` (the identical source-checked/destination-unchecked asymmetry, reachable via a symlinked top-level `.claude`/`.pi`/`.agents` in a worktree) was confirmed via `git diff`/`sha256` to be byte-identical to `main` across every commit this task made — i.e., genuinely pre-existing and untouched by this diff, not something the maker's changes introduced or could plausibly be asked to fix within this task's own reviewed scope. Excluded from every subsequent round's fix scope by explicit instruction to both the maker and the checker; flagged to the top-level coordinator as a separate, deferred follow-up task rather than silently dropped.
- **Gates:** verify-gate PASS at `d94894a69d257d6bc467cbd47fdc3863d751a457` (checked-sha match) → human hold `1442ae0c` answered `merge` — a genuine per-task decision, independently re-verified by the sub-supervisor against the raw `decisions.jsonl` ledger (`hold`/`answer` event pair, matching sha) and `bin/hold.py open` (empty) before acting, not trusted from the coordinator's relay message alone → merged to main via `bin/merge-sequencer.sh` at `e809fcd8060bae685d3f2a09d5e1fe7f001e716e`, pushed → full selfcheck chain re-run clean, sequentially (correcting an earlier concurrent-execution mistake — see flow.md), on the actual merged commit in the primary checkout → worktree/workspace `w3X`/branch torn down → claim released both times this task ended a turn (once on the `SM_STUCK_NEED_HUMAN` path, once on final completion).
- **Escalations:** one genuine hold-wait-resume cycle (opened, polled with no answer, released the claim, reported `SM_STUCK_NEED_HUMAN:1442ae0c` and ended the turn rather than guessing; resumed later by the coordinator with a genuine, independently-re-verified `merge` answer). No self-answered holds. No `AskUserQuestion`-style escalations were available in this sub-supervisor context; both judgment calls above were made directly by the sub-supervisor and disclosed in full rather than escalated or silently absorbed.

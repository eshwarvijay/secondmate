## 2026-09-10 — harden-checker-invariant: fix the documented checker-pane recipe's herdr pane run quoting bug + add a standing SessionStart invariant

- **Maker (pi/Qwen3-Coder-Next, medium thinking):** fixed `skills/secondmate/SKILL.md`'s "Checker pane" recipe to write the checker invocation to a script file on disk (`herdr pane run <pane> bash <script-path>`) instead of the old broken inline multi-command string, and added one terse invariant line to `bin/session-activate.sh`'s existing "mandatory sequence" list stating the same requirement. Two rounds — the second a targeted one-line fix for a bug the supervisor found directly, not the checker.
- **Decisions:** (1) plan-committee run unconditionally per this repo's standing policy, adhd skipped (mechanical, closed-ended, known root cause — matches the global CLAUDE.md gate for skipping divergent ideation); (2) explicitly declined several planner-suggested scope expansions during synthesis — no temp-file cleanup/rotation scheme, no runtime `assert_single_argv` enforcement code (structurally impossible: `session-activate.sh` only fires once at SessionStart and prints static text, it cannot intercept a later tool call), no injection/path-traversal hardening (the script path/content here is always supervisor-authored literal text, not untrusted input — no new attack surface); (3) scope stayed exactly the two files named in the original ask — no changes to `bin/herdr-pane.sh` or any other SKILL.md recipe even though at least one planner flagged similar patterns might exist elsewhere (deferred as a separate task if ever discovered, not chased here).
- **Checker findings resolved:** the real cross-model checker (`gpt`) round returned a clean `pass` with zero findings on its only round — but the supervisor independently caught and fixed a real bug in the maker's FIRST commit before the checker ever ran: a quoted heredoc delimiter (`<< 'SCRIPT_EOF'`) that would have disabled `${CLAUDE_PLUGIN_ROOT}` expansion in the generated checker script, silently reintroducing a different broken-recipe failure mode in the exact fix meant to eliminate one. Verified directly with a standalone heredoc-quoting repro (quoted delimiter → literal unexpanded `${X}`; unquoted → correctly expanded) before routing the one-line fix back to the maker, and re-verified the same way after the maker's second commit switched to an unquoted delimiter, confirming `${CLAUDE_PLUGIN_ROOT}` now expands while the intentionally-escaped `\$?` correctly stays literal for the generated script's own runtime evaluation.
- **Gates:** verify-gate PASS at `90eb516` (checked-sha match) → human hold `148f20fb` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` (its first real production use) at `be4b0b0`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit, independently by the supervisor → `claude plugin validate .` passed → worktree/branch/workspace/marker torn down.
- **Escalations:** none — the merge hold was a genuine per-task ask, answered before integration. A mid-task question from the human ("where is the checker, I don't see a separate panel") was answered by explaining the actual pane lifecycle (both worktree creation and pane splits use `--no-focus` by design; the checker pane had already been read and closed per the standard per-task pane-cleanup step by the time the question was asked) — not a process gap, just a timing/visibility clarification.

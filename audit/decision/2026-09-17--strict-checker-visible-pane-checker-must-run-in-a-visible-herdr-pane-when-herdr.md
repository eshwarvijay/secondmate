## 2026-09-17 — strict-checker-visible-pane: checker must run in a visible herdr pane when HERDR_ENV=1, no headless exceptions

- **Maker (pi/Qwen3-Coder-Next, medium thinking):** made 3 verbatim-specified edits across `skills/secondmate/SKILL.md` (step 4's "Check" section, and the "Visible orchestration in herdr" section's opening paragraph) and `bin/session-activate.sh` (SessionStart supervisor-invariants item 6), elevating an existing correct-but-easy-to-miss pattern into an explicit, cross-referenced, unconditional rule: the checker must run in a visible herdr pane whenever `HERDR_ENV=1`, headless is prohibited, not just an available alternative.
- **Decisions:** the supervisor pre-resolved the exact wording for all 3 edits before handoff (verbatim before/after text in the maker prompt), given the precision the task demanded — a strict rule's own wording matters more than usual; declined a runtime-enforcement mechanism (wrapper script, pre-commit hook) as explicitly out of scope, keeping this purely documentation/hook-text; kept `bin/session-activate.sh`'s new clause as one more bolded phrase inside the EXISTING item 6, not a new numbered item, matching that list's own terse one-bullet-per-invariant convention.
- **Checker findings resolved:** none from the real checker — clean pass, zero findings. One self-correction before the checker ever ran: the maker made all three edits correctly (verified byte-for-byte against the supervisor's own spec) but never committed — caught directly via `git status`, not trusted from any completion claim, fixed with a commit-only nudge.
- **Gates:** verify-gate PASS at `5de3315` → human hold `3402a9ca` answered `merge` → merged to main via `bin/merge-sequencer.sh` at `7e9334a` → version bump (`0.1.20` → `0.1.21`) → full repo-wide selfcheck chain re-run clean (`ALL_OK`) → `claude plugin validate .` passed → torn down.
- **Escalations:** one genuine per-task merge hold (answered `merge`). No self-answered holds.

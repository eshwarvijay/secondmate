## 2026-09-11 — doctor-heal-stale-report: bin/doctor.sh's --heal command shows the pre-heal state in its own trailing table

- **Maker (pi/Qwen3-Coder-Next, medium thinking):** extracted the top-level `ROWS`/counter initialization into a new `_reset_detect_state()` helper and inserted `_reset_detect_state; detect` immediately before each `emit_table` call in both `--heal` branches (with and without `--yes`), so the printed table always reflects post-heal reality rather than the pre-heal snapshot taken by the script's original top-of-file `detect()` call. Added selfcheck Test M (the `--yes` path) and, after round 1, Test N (the interactive path). Two real fix rounds.
- **Decisions:** (1) discovered through genuine operational use, not a dedicated audit — the human asked to "install the new plugin," which surfaced both a precondition bug (the marketplace checkout itself was stale, fixed ad hoc with a plain `git pull` before this task even started) and the real `doctor.sh` bug this task fixes; (2) scoped strictly to the ordering/re-invocation fix — explicitly declined to refactor `detect()`/`add()`/`emit_table()` into a stateless design (larger blast radius than the bug warrants, breaks existing callers), and explicitly declined to touch the `--json` path (never calls `heal()`, doesn't have this bug); (3) discovered and explicitly deferred two separate issues without fixing either: a structural test-fixture limitation (a synthetic fixture can only ever reach `reload_pending` after heal, never genuine `ok`, since `running_version` reads the real script's own real plugin.json with no override) — judged the test's actual assertion ("not stale") as the correct achievable contract, not a compromise to strengthen; and a separate, pre-existing, already-real bug where the STATUS summary line says "needs healing" identically for both `stale` and `reload_pending`, misleading for the latter — flagged to the human, left for a future task.
- **Checker findings resolved:** round 1 found one real, confirmed gap — the fix touched both `--heal` branches but only the `--yes` path had regression coverage (Test M); the interactive branch's fix was real but unverified. Closed with Test N, and the supervisor independently re-verified via an ISOLATED mutation (reverting only the non-`--yes` branch's insertion) that Test N specifically fails while Test M still passes — proving it's a real, independent guard, not vacuous or redundant. Round 2: clean pass, zero findings, after the checker independently re-confirmed Test N's piped stdin input (`y\n`) actually matches `_heal_secondmate`'s real confirmation-prompt contract rather than merely happening to pass.
- **Gates:** verify-gate PASS at `7514bd8` (checked-sha match) → human hold `0a438061` answered `merge` (genuine per-task `AskUserQuestion`) → merged to main via `bin/merge-sequencer.sh` (its third real production use) at `623bb8a`, pushed → full repo-wide selfcheck chain re-run clean (`ALL_OK`) on the actual merged commit, independently by the supervisor → `claude plugin validate .` passed → torn down.
- **Escalations:** one genuine per-task merge hold (answered `merge`). No self-answered holds.
